# Ampere Battery Lab — AI project instructions

Read this file before changing code, release metadata, analytics, store text, or tester-recruitment material. The detailed project map is in `docs/AI-PROJECT-CONTEXT.md`.

## Project identity

- Product: Ampere Battery Lab, an Android-first local battery-analysis app.
- Android package and iOS bundle ID: `com.ampere.batterylab`.
- Android source: `android/`; iOS port: `ios/`; web prototype: `prototypes/web/`.
- Battery readings, history, sessions, and telemetry remain on-device. There is no Ampere application server.
- Public binary/update repository: `https://github.com/Apfelkringel/ampere-battery-lab-updates`.

## Safety and privacy rules

- Never commit signing keys, passwords, service-account JSON, API secrets, personal tester email addresses, or private analytics exports.
- Do not add analytics for battery values, foreground-app names, precise location, account identifiers, or free-form user content.
- Analytics is opt-in only. Preserve default-off manifest flags, consent storage, Firebase consent, and reset-on-withdrawal behavior.
- Never claim live Google Analytics numbers without an authenticated source, query period, filters, and freshness.

## Current verified implementation

- Android compile/target SDK: 37; min SDK: 23.
- Distribution flavors: `direct` and `play`; direct may use the signed GitHub APK update flow, while Play relies on Google Play updates.
- Firebase Analytics uses Firebase BoM `34.19.0`; `android/app/google-services.json` is public project configuration, not a secret or Console access.
- The published direct APK/AAB is `versionCode 490` / `versionName "0.490"`. The active Play Alpha is `0.489` (`versionCode 489`), available to selected closed testers; code `459` remains in the track as the compatibility fallback. Do not replace the active Alpha during the closed-test gate without a fresh continuity check and explicit release authorization.

## Analytics contract

Implementation: `android/app/src/main/java/com/ampere/batterylab/AnalyticsTracker.java`.

Consent is stored separately in `ampere-privacy`, outside exported/restored battery preferences. Collection is disabled until enabled by the user; disabling calls `resetAnalyticsData()`.

| Event | Parameters | Meaning |
| --- | --- | --- |
| `ampere_section_view` | `section`: `overview`, `charge`, `discharge`, `battery_health`, `history` | A section opened after consent. |
| `ampere_feature_used` | `feature`: `health_measurement`, `history_export`, `charge_alarm`, `discharge_alarm`, `temperature_alarm`, `overlay` | An allow-listed feature activated after consent. |

Automatic screen reporting is disabled. Ad storage, ad user data, and ad personalization are explicitly denied. Do not broaden this vocabulary without updating privacy text, Play Data Safety, tests, and this document.

Repository inspection establishes schema and privacy boundaries, but not active users, retention, conversion, or event counts. For those metrics, use the authenticated Firebase/Google Analytics property and record property/stream, UTC date range, timezone, consent population, filters, freshness, and sampling/modeling status. If unavailable, report the limitation rather than guessing or substituting Reddit activity.

## Architecture map

- `MainActivity` is the lifecycle/UI adapter and canvas-style dashboard renderer.
- `BatteryDataRepository` is the persistence seam; current storage uses `SharedPreferences`.
- `BatterySamplingPolicy` owns sampling interval, rollback handling, and about 30-day telemetry retention.
- `BatteryMonitorService` collects validated snapshots and system events on a background thread.
- `BatteryBackupCodec` owns backup/restore allowlists and type encoding.
- `BatteryTelemetryDiagnostics`, `BatteryExportRules`, and `Battery*Rules` centralize validation and deterministic exports.
- Read `docs/ARCHITECTURE.md` and `docs/PROJECT-STRUCTURE.md` when boundaries change.

## Verification commands

Use Java 17 and explicit flavor/task names from `android/`:

```sh
gradle --no-daemon :app:testDirectDebugUnitTest :app:lintDirectDebug :app:assembleDirectDebug
gradle --no-daemon :app:testPlayDebugUnitTest :app:lintPlayDebug :app:assemblePlayDebug
```

Release tasks require private signing credentials and must fail rather than fall back to a debug key. Analytics changes need tests for consent persistence, allow-listed events, denial/reset behavior, and parameter boundaries.

## Release completion rule

User-visible Ampere changes are not complete when source code is merely pushed. Unless the user explicitly says otherwise, finish by:

1. incrementing Android app version code/name;
2. running relevant tests and lint;
3. publishing the signed, tagged release;
4. updating the public APK and `latest.json` in `ampere-battery-lab-updates`;
5. verifying public APK package/version and SHA-256 against `latest.json`.

Follow `docs/STORE-PUBLISHING.md`, `docs/UPDATE-SECURITY.md`, and `tooling/release-ampere/README.md`. Never expose signing material in logs.

## Working conventions

- Preserve unrelated user changes and inspect `git status` before editing.
- Prefer small, testable Java rule classes over adding logic to the renderer.
- Keep unavailable measurements explicitly unavailable; never invent zeroes.
- Keep German and English text pairs in `AppText`; localize text baked into graphics with locale-qualified drawable variants and use localized string resources for widget placeholders.
- Keep comma-delimited session storage delimiter-safe in both locales; validate German and English duration formats and migrate previously saved English dates containing commas before rendering or export.
- Translate complete German labels before fragment replacements; leave already-English sentences intact, localize persisted status/source values at display boundaries, preserve externally supplied app names, and review every rendered English screen for fragments the marker-based translator can miss.
- Keep both Android locales in each Play install for the app-specific language switch; do not enable language splits unless on-demand locale delivery is implemented and verified.
- The iOS app follows the system language and keeps German/English entries in `ios/Ampere/{de,en}.lproj/Localizable.strings`; localize dynamic SwiftUI text, status values, sharing summaries, and accessibility labels explicitly.
- Large-text navigation labels/actions live in `BatteryLargeTextTabs`; preserve tested translations, selected-tab accessibility descriptions, and automatic scroll-to-selected behavior when changing the reflowable dashboard.
- TalkBack live summaries must localize generated labels, states, estimate sources, and units in both languages, including in large-text mode; keep representative German-origin English cases covered by unit tests.
- The compact Home capacity-measurement button must immediately show the active state and stop action after a successful tap. Share its geometry across drawing, touch, and TalkBack, keep the accessible target at least 48 dp, and route every start through the same unplugged-at-or-below-25% guard.
- Keep alarm thresholds, sampling options, session-detail text, history-chart accessibility fallbacks, and ongoing-notification number formats in the selected app language.
- Use `AppText.uiLocale(context)` for user-visible decimal measurements, chart scales, energy/power labels, charger limits, and chart time ranges; keep machine-readable exports locale-independent.
- Localize human-readable diagnostic reports and the optional charging screensaver using the selected app language; keep CSV/JSON schemas and machine-readable values locale-independent.
- Public update manifests should include `releaseNotes_en` alongside `releaseNotes`; the English UI must not display German release notes when no English version exists.
- Check accessibility, narrow layouts, landscape, backup/restore, and both distribution flavors for UI or data-flow changes.
- Record new durable facts in `docs/AI-PROJECT-CONTEXT.md` and update this file when an instruction or invariant changes.

## Tester, Reddit, and document maintenance

- For an explicit request to optimize Play Console autonomously, maintain a prioritized, evidence-based backlog and complete reversible, draft-only improvements when their effect is clear. Recheck the authenticated Console before acting; record the source page, timestamp, exact before/after, and whether a change is draft, approved, or live in both project context files.
- Autonomous Play Console optimization does not authorize publishing Store listings, Alpha, Internal, or Production; changing tester-track enrollment; accepting legal/device-catalog terms; enabling device filters that may reduce tester reach; spending on Ads; deleting notifications; or starting experiments on a statistically tiny sample. The user's explicit request to make `0.489` available to closed testers authorized Submission 62 only. After the user confirmed the exact scope, that submission's Alpha release and 31 Store-entry changes were published together on 2026-09-24; this one-time authorization is consumed. It does not authorize future Alpha or listing publications. Internal-test opt-in makes a user ineligible for open/closed tests; never route required closed testers to Internal.
- One-time authorization update (2026-09-24): after being told Submission 62 bundled Alpha `0.489` with 31 Store-entry changes, the user explicitly authorized publishing all 32. That exact bundle was published. This one-time override applies only to those Store-entry changes; it is not standing authority for future listings, Internal, Open testing, or Production.
- First autonomous Play improvement (2026-09-24 18:09 UTC / 20:09 Berlin): corrected Finnish (`fi-FI`) short-description punctuation in a separate Store-listing draft. Saved text: `Akun käyttö, lataustila ja historia — paikallinen, läpinäkyvä, ei tiliä.` (72/80); reopening the editor showed the corrected value without the dash-policy warning. This is draft-only. The 31 already approved/publishable locale changes remain unchanged and must not be assumed to contain this correction.
- Fresh tester/release check (2026-09-24 18:09 UTC / 20:09 Berlin): Dashboard says 12 testers for 2 consecutive days; the 14-day requirement is incomplete and Production access is disabled. Closed Alpha remains `0.459` (last updated 17 Sep). Internal Testing is `Inaktiv`, setup 1/3, and its `0.384`-labelled draft contains codes 489, 488, and 467; code 489 is not downloadable through Play by testers yet. Do not activate Internal or replace Alpha without an explicit release decision and continuity review.
- Fresh listing/notification check (2026-09-24 18:09 UTC / 20:09 Berlin): managed publishing is enabled, last live publication remains 22 Sep, and the Publishing overview still offers `31 Änderungen veröffentlichen`. The `es-ES` queue row still summarizes the name as `Laboratorio de baterías Ampere`, while the selected editor showed `Ampere Battery Lab`; treat this as unresolved and keep the batch unpublished. Notification center still has the unread 24 Sep approval notice plus the known 19 Sep SDK warnings; read the approval text, did not delete notifications or publish the approved changes. The Store-entry report carried the retained period 2026-08-23–2026-09-19: 3 visitors, 2 single-user install clicks, 67% click-through; the standard-entry row showed 1 visitor and conversion `-`. Do not combine these surfaces into a funnel or start an experiment from this sparse sample.

- The current closed-test objective is to maintain at least 12 continuously opted-in Google Play testers for the required 14-day period before requesting production access. Treat the Play Console dashboard as authoritative; Reddit replies, group membership, and chat messages are only supporting evidence.
- Whenever Reddit is opened for tester recruitment, first inspect notifications, red markers, inbox messages, and chat requests. Read the full message before responding. Accept relevant tester/test-for-test chat requests, keep coordination public where practical, and never request or publish tester email addresses.
- For each tester who joins, send the official Ampere group, opt-in, and install links; ask the tester to remain opted in and keep the app installed for the full period. Do not promise ratings or positive reviews.
- Before claiming tester-goal completion, re-check the Play Console count/status and record whether the 14-day criterion is still running or complete. Never infer the count from Reddit activity alone.
- Keep the tester-link document current and ordered per app as: opt-in/test group, then install link, then any internal-test caveat. Current document: `https://docs.google.com/document/d/10eYlUuuS6M93f26NxNXU4eu3q64PyqyCS5NyzWfYx0I/edit`.
- `AGENTS.md` is not updated by a background process. At the start of each substantive project session, inspect it and `docs/AI-PROJECT-CONTEXT.md`; after any durable change to project facts, tester workflow, analytics/privacy rules, release process, or document links, update both files in the same change.
- A twice-daily Codex thread monitor named `Ampere Play-Console-Gate überwachen` is active (`FREQ=DAILY;BYHOUR=9,18;BYMINUTE=0`, host/local Europe/Berlin schedule). It checks the current closed-test gate, active Alpha/upload status, GitHub workflow and latest public AAB, unread Play notifications, growth metrics, Store Listing tests/assets, translation/listing drafts, and Play-AI/Vitals results; it stays quiet when unchanged and updates both project context files on material changes. It has no authority to publish future Alpha or listing changes, activate Internal, Open testing, or Production, or start an experiment on a statistically tiny sample. The completed Submission 62 publication was a one-time exception only.
- Before handoff, verify that these instructions still match the current Play Console status, public update metadata, analytics implementation, and tester document. State any unavailable or stale external information explicitly instead of guessing.

### Latest Play Console inspection (2026-09-22)

- Dashboard: the closed-test release criterion and the minimum-12-testers criterion show green checks; the 14-day closed-test criterion is still in progress and Production access remains disabled.
- Closed track: `alpha`, active, 177 countries/regions, newest track release `0.459` published 18 Sep 2026. Do not replace this release during the active 14-day window without checking the effect on continuous tester eligibility.
- Tester selection: Google Group `ampere-battery-lab-testers@googlegroups.com`; Android participation and web opt-in links are visible in the track's Tester tab.
- Optimization applied: the closed track feedback channel was set to the public Reddit test thread `https://www.reddit.com/r/AndroidAppTesting/comments/1whdrqe/looking_for_12_android_testers_ampere_battery_lab/`.
- The feedback-channel change was submitted on 2026-09-22; submission activity entry 60 was published by Play Console at 18:16 (submitted 17:56 as displayed by Console). Treat the Reddit feedback channel as live.
- Submission 60 details were re-opened: source is Play Console and exactly one change is listed—changing the Alpha tester feedback channel to the public Reddit thread. No binary, version, or track-release change was included.
- Future Play Console review order: Dashboard gates → closed-track release/tester settings → pending publication changes → app content/policy declarations → store listing → statistics/quality → App integrity. Record each concrete finding here and in `docs/AI-PROJECT-CONTEXT.md`.
- Additional inspection: Policy status reports “Keine Sicherheitslücken gefunden”; App content has no declarations awaiting review.
- Store listing is live in German with category `Tools`, four phone screenshots, no video, no website contact, and no selected Play Store tags. The current Growth overview (22 Sep 2026, last 28 days) shows 5 device impressions, 0% delta, and no available device-acquisition/first-open/MAU/7-day-retention data; the last-90-days Store-entry card shows 66.67% conversion. Treat the conversion as an extremely sparse signal, not a stable KPI. Do not start a Store-listing experiment or invent tags/copy until traffic is materially larger.
- Store-listing validation was rechecked on 2026-09-22: the German default listing reached the review step without a visible blocking validation error. Current assets are one icon, one feature graphic, and four phone screenshots; no video or tablet/Chromebook/XR assets are configured. A free Google Play machine-translation order was completed on 2026-09-22 for 28 languages (order `7430adcd1v2`, 0.00 USD, source en-US, Store entry only). The generated listings were reviewed in the Console and accepted as drafts; the brand name was normalized back to `Ampere Battery Lab` and overlong machine-generated short descriptions were shortened to the 80-character limit. The publication overview then showed 31 changes (28 locale additions plus existing German copy/media drafts); all 31 were moved to `Für später gespeichert`, leaving the review-submission queue empty. They are not live publication and must remain deferred until provenance and language quality are checked.
- Play protection recheck (2026-09-22): automatic protection is 1/1 active; Google Play Store protection is 6/7 active; and Google Play installations are 100.0% in the displayed 30-day card, with unknown sharing shown as `-`. Play Integrity is 0/7 active and explicitly not integrated; enabling it requires an SDK/backend integration and a new release, so it is a planned engineering item, not a console toggle to change during the active closed test. Play Billing protection is 0/4 and is not relevant unless the app adds Play Billing.
- Play Integrity implementation audit (2026-09-22): the dedicated Console page requires linking at least one Google Cloud project before the settings can be changed. All seven response fields are currently `Aus`, the Integrity test control is disabled, and the Console offers only Cloud-project linking and a custom Store-entry action. Repository inspection found Firebase Analytics but no Play Integrity client, nonce/token verifier, server, Cloud Function, or other backend endpoint. Do not enable or advertise Integrity protection until the project ownership, backend architecture, privacy impact, and a new signed release are deliberately prepared.
- Notification audit (2026-09-22): the Play Console notification center was opened and read. It contains the accepted upload-key-reset notice, the two known deprecated-SDK notices for `androidx.activity:activity:1.0.0` and `androidx.fragment:fragment:1.1.0` in release `0.459`, the 18 Sep publication notice, identity confirmation, and an optional Google Ads offer. No new technical blocker was present; notifications were not deleted.
- Play Console live recheck (2026-09-23): Dashboard still marks the closed-test release and minimum-12 criteria complete, while the continuous 14-day criterion remains in progress and Production access is disabled. The notification center showed the existing 22 Sep upload-key-reset notice, the two SDK warnings scoped to Alpha `0.459`, the 18 Sep publication message, identity confirmation, and one unread 15 Sep Google Ads offer (€400 credit after €400 spend, new advertisers only). No new technical or policy alert appeared; the Ads offer was opened and no campaign was started.
- Play Console gate recheck (2026-09-23): the authenticated Dashboard now explicitly says `Momentan haben sich 12 Tester für 1 Tag angemeldet`; the release and minimum-12 checks are green, but the 14-day requirement is still incomplete and `Produktionszugriff beantragen` remains disabled. Closed testing is active with one track; Open testing, Internal testing, and Production are inactive. This is the authoritative current tester count and elapsed-gate display; recheck later rather than assuming the gate date from Reddit or group membership.
- Growth recheck (2026-09-23): last 28 days still show 6 device impressions, 2 device acquisitions, and 2 first opens (each 0% delta); monthly active devices and 7-day retention are unavailable. The separate last-90-days Store-entry card remains at 66.67% conversion. Listing tests remain 0 running, 0 completed, 0 applied. These are too sparse for experiments or broad optimization decisions.
- Notification recheck (2026-09-23): notification popup still lists the 22 Sep accepted upload-key-reset notice, both 19 Sep deprecated-SDK notices, 18 Sep publication notice, identity confirmation, and the 15 Sep Google Ads offer. No new technical alert appeared; the offer was not acted on.
- Play protection detail recheck (2026-09-22): the automatic-protection report shows 100.0% Google Play installations and no measurable unknown sharing; the Play Store protection panel confirms Play App Signing, Play Protect/SDK scanning, spam/anomaly/fairness protections, and only one missing service: Store-listing device checks. Those checks remain disabled because Google warns they can reduce visibility and require a deliberate device-coverage decision; do not enable them during tester acquisition.
- Device-check option audit (2026-09-22): the missing Store-protection service is `Geräteprüfungen für Store-Eintrag`; it is disabled and Google requires acceptance of the device-catalog terms before enabling Basic, Device (recommended), or Strict integrity filtering. Do not enable it automatically: it can reduce visibility for devices/virtual environments and could affect tester reach. Revisit after the closed-test gate with an explicit device-coverage decision.
- Advanced-distribution audit (2026-09-22): app availability is `Veröffentlicht`; the Formfaktoren page reports Android XR as active. Managed Play Store is disabled; Play-as-you-download is disabled and its controls are unavailable; carrier targeting is off and applies only to Production; App Actions is unchecked and requires policy/terms acceptance; App indexing has no confirmed websites; Inline installations are unchecked and require the Inline Install API plus eligibility. Do not enable these features without matching app/deep-link/Assistant implementations and verified product intent.
- Policy/program audit (2026-09-22): Richtlinienstatus reports `Keine Sicherheitslücken gefunden`, and App-Inhalte reports `Alles erledigt` with no declarations requiring review. `Apps Experience` is locked until a Production release exists; it is a post-gate opportunity, not an actionable closed-test setting. `Von Pädagogen empfohlen` reports `Nicht zulässig`, so no education-program work should be planned for this app.
- Play App-Optimierung (2026-09-22): Google Play Console's `App-Optimierung` was enabled and confirmed saved. Google Play will optimize APKs generated from future App Bundles for performance, size, and security; Play explicitly states that existing releases and version drafts are not retroactively changed. This is a safe distribution optimization and does not alter the active Alpha release.
- Store metadata cleanup (2026-09-22): Play's default German listing flagged the short description because it used a Halbgeviertstrich (`–`) where the policy check requires a Geviertstrich (`—`). The draft was corrected to `Batterienutzung, Ladestatus und Verlauf — lokal, transparent, ohne Konto.` (73/80 characters), saved, and the review step now shows the corrected text without that warning. It remains an unsubmitted draft; no publication was triggered.
- Metadata source sync (2026-09-22): `store/google-play/listing/short-description.txt` now contains the exact 73-character German Play draft, including the required Geviertstrich. Keep this source file and the Play default listing synchronized before future Store-entry submissions.
- Metadata regression guard (2026-09-22): `tooling/validate-google-play-metadata.sh` now enforces Play's 1–30 app-name and 1–80 short-description limits, requires the Play-approved em dash, rejects en-dash/double-hyphen variants, and runs before Android tests in `.github/workflows/build-apk.yml`. Run it locally before changing Store copy.
- Fast metadata CI (2026-09-22): `.github/workflows/validate-play-metadata.yml` runs the same guard on relevant pushes, pull requests, and manual dispatches, so listing regressions are caught without waiting for a signed Android build.
- Metadata source sync (2026-09-22): `store/google-play/listing/app-name.txt` now records the verified 18-character name `Ampere Battery Lab`; the local guard validates it against Play's 30-character limit.
- Fast metadata CI verification (2026-09-22): push run `35761077182` completed successfully on commit `a36bdc7`.
- App-name guard verification (2026-09-22): push run `35761445345` completed successfully on commit `14a3909`; it validated app name `18/30` and short description `73/80`.
- CI verification (2026-09-22): GitHub Actions run `35760526133` passed the new metadata guard (`73/80`), direct unit tests/lint, signed direct APK build, Play App Bundle build, certificate-lineage verification, provenance attestations, and artifact upload. GitHub emitted only its existing Node.js 20/ubuntu-latest migration notices and APK/JAR metadata signature warnings; the job completed successfully.
- CI action maintenance (2026-09-22): updated the pinned GitHub Actions used by Android, iOS, TestFlight, and metadata workflows to the verified current releases: checkout `v7.0.1`, setup-java `v6.0.1`, setup-gradle `v6.3.0`, setup-android `v4.0.4`, upload-artifact `v7.0.1`, and attest-build-provenance `v4.2.2`. Re-run the Android build before treating the migration as complete.
- CI action migration verification (2026-09-22): Android build run `35761765426` passed metadata validation, tests/lint, signed APK/AAB builds, certificate-lineage checks, attestations, and artifact upload with the new pins. The prior Node.js 20 action warnings disappeared; only the independent future `ubuntu-latest` runner migration notice remains.
- Runner stability (2026-09-22): Linux workflows now pin `runs-on: ubuntu-24.04` for Android builds, Play publishing, and metadata validation. This prevents the announced automatic `ubuntu-latest` migration to Ubuntu 26 from changing release behavior unexpectedly; iOS workflows keep their explicit `xcode-27` runner.
- Runner verification (2026-09-22): full Android build run `35762509814` succeeded on the pinned Ubuntu 24.04 runner for commit `7e48bda`. Metadata validation, unit tests/lint, signed APK/AAB builds, certificate-lineage verification, provenance attestations, and artifact upload all passed; release attachment was skipped as expected because the dispatch was from `main`, not a tag. The prior Ubuntu migration notice is no longer present; remaining warnings are SDK-manager, Gradle, and JAR-signature metadata warnings only.
- Verification pass (2026-09-22): `gradle --no-daemon :app:testDirectDebugUnitTest :app:lintDirectDebug :app:testPlayDebugUnitTest :app:lintPlayDebug` completed successfully. Gradle reported only the existing SDK XML version-4 compatibility warning and deprecation/configuration-cache notices; no test or lint failure occurred.
- Security/release alert: Play Console showed an unread 22 Sep 2026 notification that the intentionally initiated upload-key reset for `com.ampere.batterylab` was accepted. The new upload certificate is announced as valid from 24 Sep 2026 15:37 UTC and uploads are blocked until then. Its SHA-1 matches the tracked `android/ampere-upload-certificate.pem` exactly; do not request a second reset or expose private key material.
- Upload evidence: a real internal-track attempt on 22 Sep passed the certificate-file preflight but Google Play rejected it with “The upload certificate this APK is signed with is not yet valid because it has been recently reset.” The Console activation time is authoritative over the certificate PEM `notBefore` timestamp; do not retry before 24 Sep 2026 15:37 UTC.
- Release automation inspection: `.github/workflows/publish-google-play.yml` is active on GitHub and runs every 15 minutes, verifies the public `latest.json`/AAB hash, validates the Play publisher credential, checks the target track through a temporary Play edit, and skips an upload when the version code is already present. Its certificate preflight honors both the tracked public certificate `notBefore` timestamp and the Play Console activation deadline `2026-09-24T15:37:00Z`, preventing repeated “recently reset” failures. The latest verified public manifest is now version `0.488`/code `488`; the scheduled Play upload remains separate from the public direct release. The schedule defaults to the `internal` track and must not be changed to `alpha` or `production` without explicit release intent.
- Upload workflow hardening (2026-09-22): `.github/workflows/publish-google-play.yml` uses `0.488` as the manual-dispatch example/default, matching the verified public manifest, serializes scheduled/manual runs with a shared concurrency group, and applies a 15-minute job timeout. This prevents stale manual defaults and overlapping upload attempts while preserving the safe `internal` default track.
- Upload workflow regression found and fixed (2026-09-23): scheduled GitHub runs do not receive `workflow_dispatch` inputs, so the published workflow fell back to `0.487` and `completed` even though the manual form displayed `0.488` and `draft`. Runs `35826528277` and `35857598086` failed in the initial public-bundle version check with `RELEASE_VERSION: 0.487`; no Play upload step ran and no Play state changed. Commit `5be63d9` aligns the runtime fallbacks with `0.488` and `draft`. Validation-only run `35867880435` on that commit succeeded: public bundle/hash and Play API access passed, the track stayed `internal`, and the upload-key preflight safely skipped the upload until `2026-09-24T15:37:00Z`.
- Key-window recheck (2026-09-24 14:46 UTC / 16:46 Berlin): Play Console's accepted upload-key reset still states activation at `2026-09-24T15:37:00Z` (17:37 Berlin), so the key is not yet eligible at this check; approximately 51 minutes remain. GitHub Actions still lists run `35985549783` (#121, scheduled, success, 24 Sep 12:09 GMT+2 / 10:09 UTC) as the newest run. Its steps confirm artifact/hash checks, Play publisher credential validation, and the certificate wait preflight passed; `Upload bundle to Google Play` was skipped, and no post-activation upload has yet been evidenced. Keep the built-in schedule and guard; verify an actual run and the `internal` draft after activation. No Alpha/Production release is authorized.
- Validation-only run during key wait (2026-09-24 14:56 UTC): because the latest scheduled run visible was still #121 from 10:09 UTC, manually dispatched run `36016439739` (#122) with version `0.488`, track `internal`, status `draft`, `validate_only=true`, and replacement override false. It succeeded: public AAB/manifest version and SHA-256 passed; the publisher service account obtained OAuth and read-only Play API access; Play's temporary internal-track edit confirmed existing draft code `467` would be retained. The upload-key step still reported the Console activation deadline `2026-09-24T15:37:00Z` as not yet reached; the upload action was skipped (both validate-only and key guard). The temporary Play edit was deleted by the workflow; no track/release was changed. Recheck after activation and confirm any upload on the internal draft only.
- Spanish listing verification (2026-09-24 15:12 UTC): opened the live Play editor for `es-ES`. Its App name field is exactly `Ampere Battery Lab` (18/30); Spanish short description is 62/80 characters, and the localized full description is present. This confirms the current editor does not translate the brand name, despite the Publishing queue's stale-looking row summary `Laboratorio de baterías Ampere`. The editor showed no unsaved changes; did not edit, save, or submit this listing. Keep the 31-change translation batch deferred and inspect the Publishing change details before submission; the editor alone does not prove the queued payload matches it.
- Scheduled Play workflow resumed (2026-09-24 15:13 UTC): GitHub run `36018590931` (#123) was triggered by `schedule` at 15:13:34 UTC (about 6m34s after the `:07` cron slot) and completed successfully. The public AAB/hash, Play OAuth/read-only API access, and internal draft preservation (`467`) passed; the upload step was skipped because the Console key remains unavailable until `15:37:00Z`. This resolves the immediate gap of no run since 10:09 UTC but also confirms cron timing is delayed/irregular; do not assume every 15-minute slot executes. The next useful checkpoint is the first completed scheduled run after 15:37 UTC, followed by a fresh internal-track check.
- Deferred Store-change queue safeguard repeat (2026-09-24 14:46 UTC): Publishing overview again exposed the 31 locale/listing edits as ready to submit, including the unresolved Spanish app-name summary `Laboratorio de baterías Ampere`. No content was submitted. Reapplied `Für später speichern`; the Console confirmed `31 Änderungen für später gespeichert` and returned to the no-ready-changes state. The recurrent queue resurfacing means every Play Console check must verify the review queue and re-defer this exact Store-entry batch until its locale content and Spanish-name discrepancy are resolved.
- Track-release preservation (2026-09-23): The Play Console confirms Internal Testing is inactive and contains only saved draft `0.384`/code `467` (20,319 supported devices; setup 1/3). The pinned upload action supports `versionCodesToRetain` and combines those codes with the uploaded bundle in its new draft release. `.github/workflows/publish-google-play.yml` retains differing codes automatically only when every conflicting release is `draft` and the requested new status is `draft`; any non-draft conflict skips by default, with manual `allow_track_release_replacement=true` still required after reviewing track and tester continuity. This preserves the old bundle code but groups it with the new draft rather than preserving a separate draft entry. YAML and jq matrix checks passed. Validation-only run `35877839633` on commit `9e4c628` succeeded; its log confirmed `VERSION_CODES_TO_RETAIN: 467` and the expected upload-key skip through `2026-09-24T15:37:00Z`. No bundle was uploaded and no Play Console state was changed.
- Current live gate recheck: Dashboard still shows the production-access button disabled because the 14-day closed-test criterion is incomplete. Submission 60 is published; the Dashboard has no newly submitted changes. Internal Testing remains inactive; public `0.488` is not yet verified on that track. Wait until the Console activation deadline has passed, then verify the scheduled internal upload and track state.
- Internal-track detail recheck (2026-09-23): the authenticated Console still shows Internal Testing `Inaktiv`, draft release `0.384` with version code `467` (artifact `4860231764308114663`, 20,319 supported devices), and setup 1/3. No tester track is active. Do not send the bundle to Alpha/Production or treat public `0.488` as an active Play release. Validation-only run `35877839633` proved the new workflow passes code `467` for retention while skipping the upload because the key is not active before `2026-09-24 15:37 UTC`. The next verification point is the first scheduled run after that Console deadline; confirm the track draft contains the new bundle and retained code before treating the workflow as healthy.
- Play growth and gate recheck (2026-09-23): Dashboard still marks the closed-test release and minimum-12 signups complete, the continuous 14-day requirement incomplete, and the Production-access button disabled; update status remains “Noch nicht zur Überprüfung gesendet” and Production is inactive. In the last-28-days Growth overview Play displays 6 device impressions, 2 device acquisitions, and 2 first opens (each 0% delta); monthly active devices and 7-day retention are unavailable. Its separate last-90-days Store-entry card displays 66.67% conversion. Store Listings' detailed 28-day report (`2026-08-23`–`2026-09-19`) displays 3 visitors, 2 single-user install clicks, and 67% click-through rate; the standard-listing row separately displays 1 visitor and conversion `-`. The differing surfaces/counts must not be combined into a funnel without a verified common basis. App Statistics (28-day range ending `2026-09-22`) lists 2 installed-app users on 17 and 19 Sep (Chile and Italy); this is not the closed-test opt-in count. There are no active or completed Store Listing tests. Notifications still show the known 22 Sep upload-key reset, two 19 Sep SDK notices, 18 Sep release notice, 15 Sep identity confirmation, and 15 Sep Ads offer; no new technical alert. No post-fix scheduled GitHub run is listed since successful validation-only run `35867880435`. With traffic this small, do not start an experiment, add speculative targeting, or publish deferred translation/listing drafts.
- Store-change queue safeguard (2026-09-23): although the prior verified state had 31 changes in `Für später gespeichert` and an empty review queue, the live Publishing overview now showed a button to submit 31 changes. The listed items were Store-entry locale additions/drafts; no changes were submitted. Used the explicit `Für später speichern` action on the Store-entry group; Console then showed `31 Änderungen für später gespeichert` and the review queue reverted to its empty-state message. Keep all 31 machine-translation/listing drafts deferred until content quality/provenance review is complete. This action did not publish or alter the live listing.
- Feedback/rating audit (2026-09-23): the authenticated Play Console Testfeedback page showed only its explanatory empty-state content and no submitted feedback. The Ratings report for the last 28 days showed 0 users and “Es wurden keine Bewertungen gefunden, die deinen Filtern entsprechen”; there are no reviews to answer. Do not interpret absent feedback/ratings as proof of crash-free operation, satisfaction, or product quality; Android Vitals values are still unavailable. Do not ask testers for a positive rating; invite honest private test feedback through the existing channel.
- Policy/program recheck (2026-09-23): the live Policy status page says `Keine Sicherheitslücken gefunden`; App content → `Überprüfung erforderlich` says `Alles erledigt` with no declaration awaiting action. Apps Experience remains explicitly `Gesperrt` until a Production-track release is published. No policy toggle, declaration, or release action was changed; Production remains gated by the unfinished 14-day closed test.
- Google Play protection recheck (2026-09-23): the authenticated protection dashboard again shows automatic protection 1/1 active, Google Play Store protection 6/7, Play Integrity 0/7/not integrated, and Play Billing protection 0/4. The detailed Store-protection panel identifies its only missing item as `Geräteprüfungen für Store-Eintrag deaktiviert`; other visible protections remain active (Play App Signing, Play Protect scan, Play SDK scan, spam/anomaly/fairness protections). The 30-day protection card still shows 100.0% Google Play installations and unknown sharing `-`. Do not enable device checks during recruitment: the Console requires separate device-catalog terms acceptance and warns filtering may reduce device/virtual-device reach. No protection settings or legal terms were changed.
- Post-release validation (2026-09-22): manual validation run `35765901274` downloaded public `0.488`, verified its AAB hash, authenticated the Play publisher with read-only API access, and safely skipped upload because the Console activation deadline `2026-09-24T15:37:00Z` has not passed. No Play state was mutated. A fresh Dashboard recheck still shows Production inactive, Closed testing active with one track, Internal testing inactive, all release/minimum-12 checks green, and the 14-day criterion incomplete.
- Pre-Launch audit (2026-09-22): Play's Pre-Launch-Bericht page reports `Artefakte hochladen, um Pre-Launch-Berichte zu erstellen` and has no report to inspect. The technical-quality cards in the Testen-und-veröffentlichen overview are still attached to Alpha `0.459`; they are not evidence about the unreleased/public `0.488`. Do not create an extra track merely to generate a report; after the upload-key window, use the planned internal `0.488` upload and inspect the resulting report before any Play release decision.
- Play Console recheck (2026-09-22): the Dashboard still reports Store updates as not submitted, Alpha as the only active test track, Internal Testing as inactive, and Production access as disabled. The unread upload-key notification repeats the authoritative activation time `2026-09-24 15:37 UTC`; no newer blocking warning appeared. Alpha `0.459` was published 18 Sep 2026, so the earliest 14-day closed-test checkpoint is approximately 02 Oct 2026; do not request Production access before Play marks the gate complete.
- Play optimization audit (2026-09-22): `Überprüfen und optimieren` was reviewed across device catalog, test feedback, Android Vitals, policy status, and eligible programs. The device catalog is not opened because Play first presents a separate terms-of-service acceptance; do not accept legal terms implicitly. Testfeedback has no visible feedback yet, Android Vitals has no actionable crash/ANR or app-size data, policy status says `Keine Sicherheitslücken gefunden`, and Apps Experience remains locked until a production release exists.
- Store-discovery audit (2026-09-22): the live default listing shows 1 visitor and no running or completed Store Listing test. Play Store settings remain category `Tools`; the tag manager was checked with the relevant searches `Batterie` and `Monitor`, both returning no matching tag, so no speculative tag was added. Contact data contains the developer email but no website or phone; do not invent one. External marketing remains enabled.
- Translation audit (2026-09-22): the completed Google Play machine-translation order `7430adcd1v2` still covers 28 languages at `0,00 USD`. The language rows remain available for `Prüfen und übernehmen`, while the resulting listing changes are intentionally deferred in `Für später gespeichert`; do not submit the 31-change queue without a deliberate publication review.
- Closed-test recheck (2026-09-22): Alpha remains active with release `0.459` (published 18 Sep 2026) in 177 countries/regions. The Tester tab still selects `ampere-battery-lab-testers@googlegroups.com`, keeps the Reddit feedback URL, and shows the same Android/web participation links. The Dashboard's minimum-12-tester check remains green, but the Tester tab exposes no exact member/opt-in count; do not infer a numeric tester count from the group address or Reddit activity.
- SDK warning follow-up: Play Console's 19 Sep warning identifies `androidx.activity:activity:1.0.0` in release `0.459`. The current Gradle graph also inherited that version transitively through Firebase/Google Play Services, so `android/app/build.gradle` now pins stable `androidx.activity:activity:1.13.0`. `dependencyInsight` confirms `1.0.0 -> 1.13.0`; `testDirectDebugUnitTest`, `lintDirectDebug`, `testPlayDebugUnitTest`, and `lintPlayDebug` all pass. This is source-only until a deliberately versioned release is built and published.
- Technical-quality remediation (2026-09-22): Play's next-release panel also flagged transitive `androidx.fragment:fragment:1.1.0` and Android 15 edge-to-edge APIs in Alpha `0.459`. `android/app/build.gradle` now pins stable Fragment `1.9.0`; `dependencyInsight` resolves both requested `1.1.0` and `1.0.0` to `1.9.0`. `MainActivity` no longer calls Android 15-disabled `setStatusBarColor`, `setNavigationBarColor`, or `setDecorFitsSystemWindows`; its existing API-35 `WindowInsets` padding remains in place, and theme background replaces deprecated bar-color resources. Direct/Play unit tests, lint, and debug assemblies passed; signed release `0.488` is now published on the public direct/AAB channel, while Play Alpha remains `0.459`.
- Release verification (2026-09-22): signed tag `v0.488` and GitHub Actions run `35764669114` passed metadata validation, Direct/Play tests and lint, signed APK/AAB builds, certificate-lineage verification, provenance attestations, and release attachment. Public update repository commits `20bff3b`, `0ce7ec2`, `86ceb4f`, and `6b74a7a` publish the APK, AAB, `latest.json`, and checksums. Fresh public downloads verified package `com.ampere.batterylab`, version code/name `488`/`0.488`, APK SHA-256 `c2acbfcbf86d40481d5698ba8add3cdbe2d806ef5987d9cc6c2fc0c4f798fec4`, and AAB SHA-256 `6ca8499a35fb3669b7fc505ce45e08370eaf18901b24fd764deb5fd6bf495c37` against `latest.json`.
- Manifest/Play-policy audit (2026-09-22): target/compile SDK are 37; analytics collection and ad-ID signals are default-disabled; cleartext traffic is disabled; no location/contact/account permissions are declared; both special-use foreground services have explicit user-facing subtype properties; notifications, overlay, usage access, boot, and internet permissions match documented features. No new Play declaration should be added without checking the App content forms.
- App-content recheck (2026-09-22): Play Console shows no declarations requiring action and 11 completed declarations. The completed Data-safety summary reports 3 data types collected or shared, automatic deletion over time, and encryption in transit. This remains consistent with the current opt-in-only analytics allow-list and must be revisited if analytics fields, SDKs, permissions, or retention change.
- Play Statistics snapshot (2026-09-22): the last-28-days App statistics report showed only 2 installed-app users, on 17 and 19 Sep, split between Chile and Italy. The crash metric returned “Daten nicht verfügbar”; this is insufficient to infer stability or market fit. Keep decisions conservative until more data accumulates.
- Android Vitals recheck (2026-09-22): Play Console reports no data for user-perceived crashes, ANRs, memory, startup/rendering, battery, or permission-denial metrics; the key lost-user metric is marked “Begrenzte Datenbasis”. The App-size page likewise has no representative download-size or optimization data and no recommendations. Treat this as insufficient sample/release coverage, not as proof of zero crashes or optimal size.
- Growth/AI audit (2026-09-22): the Play growth overview showed 5 device impressions in the last 28 days, 1 Store-listing visitor, and a displayed 66.67% Store conversion rate; device acquisitions, first opens, monthly active devices, and 7-day retention were unavailable. The built-in Play AI analysis of perceived crash rate by Android version only confirmed coverage of Android 14, 15, and 16 and had no data-backed recommendation because the breakdown was unavailable. The sample is too small for a Store Listing experiment or targeting decision; the experiment setup was inspected but no test was created and no saved listing/translation changes were submitted.
- Historical store-copy state (superseded 2026-09-22): an earlier draft used `Akkuverbrauch, Ladestatus und Verlauf – lokal, transparent, ohne Konto.` (71/80 characters) and briefly showed three pending changes. This was replaced by the current synchronized 73-character German draft and the later 31-change machine-translation/listing batch documented below; do not use this historical snapshot as the current Play state.
- Machine translation caveat: Google explicitly states these translations are machine-generated and not human-reviewed. Keep the 28 locale drafts separate from the live listing until each important market is spot-checked; do not claim that Google has published them merely because the order is “Abgeschlossen”.
- Translation import safeguard: an old completed AI-translation order can still be reapplied after its locale has been edited; always inspect the current locale first and compare every field before choosing `Prüfen und übernehmen`, because that action can replace better saved draft text. Never accept the whole machine-generated payload blindly.
- Translation spot-check (2026-09-23): in order `7430adcd1v2`, Spanish (Spain) AI output had the correct brand in the 30/30 app name, but its short description was 116/80 characters and Console disabled `Übernehmen`. The current Spanish draft already has the brand `Ampere Battery Lab`, a valid 62/80 short description, and the 1,420-character full description matches the order. The modal was cancelled; no draft or listing was changed. This demonstrates why reusing an old order without comparing current locale text is unsafe.
- French locale correction (2026-09-23): the authenticated standard listing editor selected `Französisch (Frankreich) – fr-FR`, but its saved 67/80-character short description was Italian (`Batteria, carica e cronologia — locale, trasparente, senza account.`), while the full description was French. Replaced only that short-description field with `Batterie, charge et historique — mesures sur l’appareil, sans compte.` (69/80) and used `Als Entwurf speichern`. Reloading and reselecting fr-FR confirmed the corrected text persisted; no submission/publication was made. During future locale reviews, verify both the selected locale and the language of each field; character-limit compliance alone is not localization QA.
- Localization cross-check (2026-09-23): after the French correction, the saved short descriptions in Italian (`it-IT`, 67/80), Spanish (Spain, `es-ES`, 62/80), Japanese (`ja-JP`, 48/80), Korean (`ko-KR`, 57/80), Brazilian Portuguese (`pt-BR`, 60/80), Simplified Chinese (`zh-CN`, 29/80), Hindi (`hi-IN`, 57/80), Turkish (`tr-TR`, 73/80), and Vietnamese (`vi`, 65/80) were read from their explicitly selected Console locales. Each sample was in the selected language and under Play's 80-character limit; Italian's own locale is correct despite the prior Italian text mistakenly appearing in French. No additional text was changed or submitted. This is a language/length spot-check only, not native-speaker review or approval of the 28-locale batch.
- English UI/build regression follow-up (2026-09-23): current uncommitted localization work exposed a missing exact translation for `Quelle: USB-Ladegerät`, duplicate translation keys, and an English percentage-spacing expectation mismatch. Narrow source/test fixes are in the working tree. A fresh `:app:clean` followed by both flavors' unit tests, lint, and debug assemblies passed. The current `android/app/build.gradle` also contains an artifact-type attribute for `*UnitTestCompileClasspath` (`android-classes-jar`) to disambiguate AGP app variants; I did not edit that user change. After it appeared, forced recompilation/execution passed both unit-test suites: 432 Direct and 432 Play tests, zero failures/errors/skips. Local verification only: no signed release or Play upload; runtime device validation and release/track continuity review remain required before publishing.
- Translation validation pass (2026-09-22): all 28 generated locale drafts were opened in the actual Store-entry editor and checked against Play's hard 30-character app-name and 80-character short-description limits. The audit found and corrected overlong or brand-inconsistent entries in Spanish (Spain/Latin America), Czech, Hindi, Hungarian, Indonesian, French, Italian, Dutch, Polish, Portuguese (Brazil), Romanian, Russian, Swedish, Thai, Ukrainian, and Vietnamese. Final direct-editor verification is 28/28 valid: every locale uses `Ampere Battery Lab`, every app name is 18 characters, every short description is 29–80 characters, and no editor shows the limit warning. The 31 resulting listing changes were moved back to `Für später gespeichert`; none were submitted or published. Human/native review is still recommended before release.
- Play Store settings audit (2026-09-22): app type is `App`, category is `Tools`, and no tags are currently selected. Searches for the evidence-relevant terms `Batter` and `Akku` returned no available tag, so do not add unrelated tags. Store contact currently has the developer email only; no verified phone number or website is configured. External marketing is enabled; leave it enabled unless a deliberate distribution/privacy decision changes.
- Play quality and localization recheck (2026-09-23): the authenticated `Abstürze und ANRs` page for 26 Aug–23 Sep shows no listed problem entries; this is not proof of zero crashes or ANRs. The `App-Größe` page has only explanatory text and no size metrics or recommendations. Growth still shows 6 device impressions, 2 acquisitions, and 2 first opens in the last 28 days, with monthly active devices and 7-day retention unavailable; its separate last-90-days conversion card is 66.67%, while Store Listing experiments remain 0 running/0 completed/0 applied. Translation order `7430adcd1v2` remains completed for 28 machine-translated languages at 0.00 USD, with each locale still offering `Prüfen und übernehmen`; no translation was imported or published in this inspection. Continue holding experiments and publication until traffic and human/native language review support them.
- Repeated Store-publication queue recheck (2026-09-23): the Publishing overview again showed 31 unsubmitted Store-entry changes available for review, even though they had previously been moved to `Für später gespeichert`. Did not submit them; applied `Für später speichern` to the Store-entry group, and Play confirmed `31 Änderungen für später gespeichert` with the review queue empty. Treat deferral as potentially non-durable across Console refreshes and verify the queue before any release submission.
- Managed publishing safeguard (2026-09-23): Play Console's Publishing overview previously showed `Verwaltete Veröffentlichung aus`. Enabled `Verwaltete Veröffentlichung` and verified the enabled state persisted after reloading the page. Google's documented behavior holds most reviewed changes—including Store listings and full/staged rollouts—for a deliberate manual publish; exceptions include tester configuration and tester-list membership. This is an additional publication gate, not a protection for tester-track edits. The 31 locale/listing changes remained unsubmitted/deferred; enabling this setting did not submit or publish them. Dashboard gates were unchanged: minimum-12 check green, continuous 14-day criterion in progress, Production access disabled. Before any submission, recheck the queue and explicitly review what manual publication would release.
- Store queue/editor consistency recheck (2026-09-23): the 31 unsubmitted locale/listing changes resurfaced again, and the Publishing overview row summary showed the older en-dash short description. Opening that change in the default listing editor showed `Batterienutzung, Ladestatus und Verlauf — lokal, transparent, ohne Konto.` at 73/80 characters, matching `store/google-play/listing/short-description.txt`. Therefore the stale string is confirmed in the queue summary, but not in the editor field; do not claim the actual saved listing payload is wrong without a true before/after comparison. Did not submit anything; `Für später speichern` was reapplied and Play confirmed all 31 saved with the review queue empty.
- Store screenshot eligibility audit (2026-09-23): all four current phone screenshots are portrait 9:16 but only 668×1188 px (`ampere-store-start-no-status-no-system-bars.jpg`, `ampere-store-verlauf-no-status-no-system-bars.jpg`, `ampere-store-sitzung-no-status-no-system-bars.jpg`, `ampere-store-verlauf-detail-no-status-no-system-bars.jpg`). They meet the ordinary listing's 320–3840 px bounds, but Google requires four portrait screenshots at least 1080×1920 px for recommendation formats that use screenshots. The current set therefore misses a specific promotional eligibility opportunity. Do not upscale these files and call that a quality improvement; capture genuine, current app screens at native resolution and review them before replacing Store assets. No connected Android device or emulator was available during this audit, so no asset was created or uploaded. Store listing editor also confirms the current German short description is 73/80 characters and matches the approved source.
- Screenshot capture feasibility test (2026-09-23 17:45 UTC): `:app:assembleDirectDebug` passed and the existing Android 16 `ampere-api36` AVD booted with host GPU/4 GB RAM. A genuine 1080×1920 render was captured for inspection, but emulator-only readings were not suitable for Store marketing: Home showed 100%/full while Charging simultaneously showed +900 mA and 4.5 W, and Battery Health showed 5.00 V, 10 system cycles, and 0 valid sessions. These are AVD-generated values, not measurements from a representative physical device, and would be misleading as promotional evidence. An initial software-rendered boot also displayed a System UI unresponsive overlay; host-GPU reboot removed it. No screenshot was uploaded or added to source control. Restored AVD display overrides (320×640, 160 dpi), force-stopped Ampere, and shut the emulator down without saving a snapshot. To close the 1080×1920 featuring-eligibility gap, capture genuine screens from a physical device or another approved source of representative values; do not alter battery readings to stage them.
- Play upload scheduler recovery check (2026-09-24 07:00 UTC): GitHub now lists four successful `schedule` executions on commit `a74aeedf`: `35907072918` (19:06 UTC), `35927228603` (22:14 UTC), `35938926959` (00:33 UTC), and `35959280961` (05:17 UTC). The latest run passed public AAB/manifest SHA-256 validation, Play OAuth/read-only API access, and preserved internal draft code `467`; its key preflight correctly skipped upload until the Console activation deadline `2026-09-24T15:37:00Z`. This resolves the earlier observation that no post-cadence scheduled execution was visible, though the observed run intervals were irregular and do not prove every 15-minute slot is delivered. No AAB was uploaded and no Play track changed. Dashboard remains at 12 testers for 1 day, 14-day gate incomplete, Production access disabled; keep monitoring and inspect the internal draft after the key window.
- Play growth data refresh (2026-09-24 07:00 UTC): authenticated App Statistics, `Letzte 28 Tage` ending 2026-09-23, now includes 3 installed-app users on 20 Sep (Chile 1, India 1, Italy 1); rows for 17–19 Sep still show 2 users per day (Chile 1, Italy 1). The new 20 Sep row is a small app-usage signal, not the closed-test opt-in count. The Growth overview remains 6 device impressions, 2 device acquisitions, 2 first opens, monthly active devices unavailable, 7-day retention unavailable, last-90-days Store conversion 66.67%, and listing experiments 0 running/0 completed/0 applied. No experiment or targeting action is justified by this sparse sample.
- Deferred Store-change queue recheck (2026-09-24 07:00 UTC): Publishing overview again showed all 31 unsubmitted Store-entry locale changes under `Änderungen noch nicht zur Überprüfung eingereicht`, with `31 Änderungen zur Überprüfung einreichen` enabled despite prior `Für später speichern` confirmation. Managed publishing remained enabled and last publication remained 22 Sep. Did not submit; reapplied the Store-entry group's `Für später speichern`, and Console confirmed `31 Änderungen für später gespeichert` and showed the no-changes-to-submit empty state. Spanish (Spain) queue summary still says `Laboratorio de baterías Ampere`, while the previously rechecked editor showed `Ampere Battery Lab`; treat the row/editor mismatch as unresolved and do not submit the locale batch.
- Play key-window precheck (2026-09-24 13:10 UTC): scheduled run `35985549783` completed successfully at 10:09 UTC on `main`/`a74aeedf`. Its log confirms public version `0.488`, artifact/hash checks and Google OAuth/read-only Play API validation passed; internal draft code `467` was selected for retention. Upload was correctly skipped because the accepted Play Console key-activation time is still `2026-09-24T15:37:00Z`. At this check the key window is still pending; do not dispatch a real upload early. Recheck after the Console time, and inspect the internal draft before any further step. Alpha and Production remain out of scope.
- Scheduled Play-upload cadence adjustment (2026-09-23): moved the 15-minute GitHub schedule from `:00/:15/:30/:45` to `:07/:22/:37/:52` UTC. The workflow is active, but recent scheduled runs were delayed/absent while the post-fix validation-only run `35877839633` succeeded; the previous scheduled failures were on pre-fix commit `502aaeec` and failed the stale `0.487` version check before any upload. GitHub documents that scheduled runs can be delayed or dropped during high load, especially at the top of an hour. The first new off-hour slot (`15:22 UTC`) had no corresponding run visible by `15:23 UTC`; the timing improvement is therefore not yet verified. The shifted cadence avoids `:00` and still checks at the upload-key activation minute `15:37 UTC` on 24 Sep. Verify a scheduled post-change run and inspect the resulting internal draft; this must never change the workflow's safe `internal`/`draft` defaults or authorize Alpha/Production.
- Workflow scheduler follow-up (2026-09-23 16:39 UTC): the GitHub workflow API still reports `Publish Google Play bundle` as `active`; the repository is not archived/disabled, `main` is the default branch, and the new cron is present on `origin/main` (commit `66c56f7`, `7,22,37,52 * * * *`). Nevertheless, no `schedule` run appeared for any of the six slots from 15:22 through 16:37 UTC. Latest listed run remains successful validation-only dispatch `35877839633` at 14:56 UTC; latest scheduled run remains the pre-fix failure `35857598086` at 11:58 UTC on `502aaeec`. GitHub's official Actions documentation confirms scheduled events may be delayed or dropped and only run from the default branch; the verified repository prerequisites are met, but the missing runs' cause is not established. No retry/upload was triggered and Play track state was not changed. Continue checking the next scheduled slot and the first post-key-activation slot; if scheduling stays absent, consider a controlled validation-only dispatch as a temporary check, not as proof that cron recovered.
- Validation-only fallback (2026-09-23 16:46 UTC): because no scheduled run appeared after the cadence change, manually dispatched run `35891086823` on current default-branch commit `a74aeed` with `version=0.488`, `track=internal`, `status=draft`, `validate_only=true`, and replacement override false. It succeeded: public bundle/hash check passed; Play OAuth and read-only API access validated; internal draft version code `467` was retained; key preflight reported Console activation `2026-09-24T15:37:00Z` and set the upload skip; the Google Play upload step was explicitly skipped. No release was uploaded and no Play track changed. This validates the job's current credentials/artifact/read path and safe pre-deadline behavior, but does not explain or prove scheduler recovery. Fresh Dashboard still reads `12 Tester für 1 Tag`; minimum-12 and test-release checks green, 14-day gate incomplete, Production access disabled.
- Scheduler observation (2026-09-23 16:53 UTC): the `16:52 UTC` scheduled slot also had no run listed 90 seconds later; the successful 16:46 validation-only dispatch remains the newest run. The six-slot miss is now seven consecutive missed off-hour slots (15:22–16:52 UTC). The workflow is still active on default branch `main`; treat this as a recurring GitHub schedule-delivery issue, not an upload/Play credential failure. Do not change cron again without evidence; use the existing validation-only fallback for safe checks and recheck after key activation.
- Scheduler observation (2026-09-23 17:09 UTC): no scheduled run appeared for the `17:07 UTC` slot; run `35891086823` remains the latest execution. This extends the consecutive missed off-hour slots to eight (`15:22` through `17:07 UTC`). The workflow is still active and its manual validation path succeeds; no additional manual dispatch or upload was made during this check.
- Store queue persistence recheck (2026-09-23 17:12 UTC): in Publishing overview, `Verwaltete Veröffentlichung aktiviert` persisted, but a page reload again exposed all 31 deferred Store changes under `Änderungen noch nicht zur Überprüfung eingereicht` with an enabled `31 Änderungen zur Überprüfung einreichen` button. The separate `Änderungen, die veröffentlicht werden können` area was empty (no Google-approved changes). Did not submit. Reapplied the Store-Einträge row's `Für später speichern`; Console confirmed `31 Änderungen für später gespeichert`, and the submission button was replaced by that confirmation. This repeated after earlier deferrals, so the defer state is not reliably surviving reload; before any future submission, inspect every queued locale/media change and do not assume the prior defer action persists. Managed publishing is an extra post-review gate, not reason to submit the batch.
- Spanish queue/editor discrepancy (2026-09-23): the Spanish (Spain) row in Publishing overview describes its queued translation as app name `Laboratorio de baterías Ampere`, while the explicitly selected `Spanisch (Spanien) – es-ES` editor shows `Ampere Battery Lab` and the valid 62/80 short description `Batería, carga e historial — local, transparente y sin cuenta.`. The row is part of the 31-locale saved batch. This could be a stale queue summary; it does not prove which payload a review submission would use. No field was changed and nothing was submitted. Do not infer the authoritative submission payload from the row summary; resolve via the Console's reviewed change detail or a reliable before/after comparison before any future submission.
- Post-activation internal AAB upload completed (2026-09-24 16:06 UTC / 18:06 Berlin): after the accepted upload-key reset deadline `2026-09-24T15:37:00Z`, dispatched the existing guarded workflow as [run 36023806899](https://github.com/Apfelkringel/ampere-battery-lab/actions/runs/36023806899), explicitly targeting public `0.488`/code `488`, `internal`, `draft`, `validate_only=false`, replacement override false. Workflow verified the public AAB against `latest.json` SHA-256, validated Play OAuth/read access, found only internal draft code `467` and retained it, then logged successful artifact upload and committed Play Edit `05388693841868255179`. Independent authenticated Console inspection confirms Internal Testing remains `Inaktiv`; its sole draft card is still labelled `0.384`, but the version-code details link to both `488` (new Bundle Explorer artifact `4860233712402175355`) and `467` (existing artifact `4860231764308114663`), with 20,340 supported devices. This is a saved internal draft only, not a tester rollout or public release; setup remains 1/3 tasks complete. Alpha `0.459` is unchanged. The dashboard reports 12 closed-test testers for 2 consecutive days; the 14-day requirement remains incomplete and Production access is disabled. Publishing overview still has 31 deferred listing/translation changes; keep them deferred until the queued Spanish brand-name discrepancy and locale quality are resolved. Do not activate Internal Testing or publish to Alpha/Production without a separate explicit release decision and tester-continuity review.
- Public direct release `0.489` (2026-09-24 16:09 UTC): signed tag `v0.489`, source build [run 36025102250](https://github.com/Apfelkringel/ampere-battery-lab/actions/runs/36025102250), and update-repository commit `e6b1b45` published the bilingual manifest and APK/AAB. CI passed metadata validation, Direct tests/lint, signed APK and Play AAB builds, signer-lineage verification, provenance attestations, and release attachment. Fresh downloads verified package `com.ampere.batterylab`, code/name `489`/`0.489`, APK SHA-256 `239b054cc1ae4d613ebc14cc6508e1de9112e8adc94de8a70f96a44fa7b7d29e`, and AAB SHA-256 `6c658e554788aa6bc75041f2f79a355bcc5293def03472b38dfc4cca27a5f69a` against public `latest.json`. English and German release notes are both present. This is a public direct/update-channel release; it does not publish to Play Alpha or activate the internal draft.
- Android developer verification key added (2026-09-24 16:17 UTC / 18:17 Berlin): the authenticated Console's developer-verification page listed `com.ampere.batterylab` as `Registriert` with three `Bestätigt` fingerprints, but none matched the current direct-release public certificate documented in `docs/UPDATE-SECURITY.md` and pinned by the release process. Added the matching public SHA-256 certificate fingerprint `FA:29:B8:75:95:EF:1B:34:B2:06:9D:1E:28:42:D2:11:4B:15:52:A0:74:E0:22:EE:52:7A:7E:C1:7E:98:1A:D3` in the Play Console's package-key manager. The package now displays four fingerprints; the new fingerprint is explicitly `Wird überprüft`, while the original three remain `Bestätigt`. This is a pending public-key association, not a private-key upload; recheck for `Bestätigt` and whether Google requests additional ownership evidence. No release or track was changed.
- Android developer-key verification completed (2026-09-24 16:22 UTC / 18:22 Berlin): rechecked the package-key manager and the new direct-release certificate fingerprint `FA:29:B8:75:95:EF:1B:34:B2:06:9D:1E:28:42:D2:11:4B:15:52:A0:74:E0:22:EE:52:7A:7E:C1:7E:98:1A:D3` is now `Bestätigt`; all four registered package keys are confirmed. The developer-verification app-list banner says all Google Play apps are registered. This resolves the earlier pending-review state.
- Play 0.489 staged internally (2026-09-24 16:34 UTC / 18:34 Berlin): ran guarded workflow [36028309019](https://github.com/Apfelkringel/ampere-battery-lab/actions/runs/36028309019), explicitly `0.489`, track `internal`, status `draft`, validate-only false, replacement override false. Public AAB/version/hash, Play credential and activated upload-key checks passed; the workflow retained draft codes `467,488`, uploaded the new AAB, and committed Play Edit `05370575271627213330`. Independent Console verification shows Internal Testing remains `Inaktiv`, setup 1/3, and its `0.384`-labelled draft contains codes `489` (artifact `4860233721511506227`), `488` (artifact `4860233712402175355`), and `467` (artifact `4860231764308114663`), supporting 20,367 devices. This is an inactive internal draft, not a tester rollout. Alpha `0.459` remains available in 177/177 regions; Production remains inactive. Do not activate Internal or change Alpha/Production without a separate release decision and continuity review.
- Play gate/growth/quality recheck (2026-09-24 16:22 UTC / 18:22 Berlin): Dashboard still says 12 closed-test testers for 2 consecutive days; minimum-12 and active-test-release checks are complete, the 14-day criterion is incomplete, and Production access is disabled. Growth overview for the last 28 days shows 6 device impressions, 2 acquisitions, and 2 first opens (each 0% delta); monthly active devices and 7-day retention remain unavailable; the separate last-90-day Store conversion is 66.67%. Store-listing experiments remain 0 running/0 completed/0 applied. Crash/ANR view showed no active problem rows in its current 28-day view; do not infer crash-free status from the absence of surfaced rows. Pre-Launch report still displays `Artefakte hochladen, um Pre-Launch-Berichte zu erstellen`; no report was available shortly after the internal 0.489 draft upload.
- New Store-entry submission and unread approval alert (2026-09-24 16:22 UTC / 18:22 Berlin): Console notification center has a new unread 24 Sep notice, `Dein letztes App-Update wurde genehmigt und du kannst es jetzt veröffentlichen`. Submission activity identifies entry `61`, submitted 24 Sep at 17:57 Berlin, type `Store-Eintrag`, status `Bereit zur Veröffentlichung`, containing 31 changes. Publishing overview has managed publishing enabled, a `31 Änderungen veröffentlichen` action, and still shows last live publication as 22 Sep. Submission 61 details confirm the Spanish (Spain) `es-ES` app name is actually `Laboratorio de baterías Ampere`, whereas the previously inspected live editor showed `Ampere Battery Lab`; this is an actual approved queued-payload mismatch, not merely stale row text. This agent did not submit or publish submission 61. Leave it unpublished until the Spanish brand name is corrected and all 31 machine-translated listings are quality-reviewed; the approval notification is not authorization to publish.
- Play upload schedule recheck (2026-09-24 16:33 UTC): latest scheduled run remained `36018590931` at 15:13 UTC; the latest manual run before the 0.489 sync was `36023806899` at 15:56 UTC. No post-activation scheduled run was visible. The 0.489 internal upload used the existing guarded manual path and succeeded, but this does not prove the cron cadence has recovered.
- Closed-Alpha 0.489 submission (2026-09-24 18:42 UTC / 20:42 Berlin): the user explicitly requested that the newest version be available to closed testers. The authenticated Dashboard showed 12 opted-in testers for 2 consecutive days at the 18:22 Berlin check; the 14-day gate remained incomplete and Production access disabled. GitHub run `36028309019` had already uploaded public AAB `0.489`/code `489` as an inactive internal draft, so a second upload attempt to Alpha was correctly rejected as a duplicate version code (`36041284962`) without changing any track. Used Play Console's existing bundle library instead: created the Alpha release from code `489` and retained active code `459` as a compatibility fallback. The release preview showed 0 devices losing support; new bundle code 489 requires API 24+, while existing 0.459 supports API 23+. Only nonblocking missing native-debug-symbol warnings were shown. Submitted exactly this one Alpha release as Submission `62`, named `489 (0.489)`, full rollout; status at 18:42 UTC was `Wird überprüft`. Managed publishing remains enabled, so approval alone will not make 0.489 live; after approval, manually publish only Submission 62 after verifying it still contains the single intended Alpha release. The user request authorizes that exact closed-Alpha publication, but does not authorize Production, Internal-track activation, or Submission 61 / the 31 Store-listing changes. Until Google's review and the final managed-publish action complete, closed testers can still receive only live Alpha `0.459`; no claim that 0.489 is downloadable yet.
- Play optimization recheck (2026-09-24 18:49–19:01 UTC / 20:49–21:01 Berlin): fresh submission activity still lists `62` as `Geschlossener Test - Alpha` / `Wird überprüft`; its details show exactly one change, `489 (0.489) – Vollständigen Roll-out starten`. Dashboard still says 12 testers for 2 consecutive days, the 14-day gate incomplete, and the Production-access action disabled. Thus 0.489 is not yet available through Play. The unread 24 Sep approval notice is still visible, but submission 61—not 62—is the Store-entry batch marked `Bereit zur Veröffentlichung`; the notice is not evidence that 62 has passed review.
- Separate ready-to-publish listing batch (2026-09-24 18:49 UTC): the fresh Publishing overview shows `31 Änderungen veröffentlichen` for Store entries and separately shows Alpha 0.489 under review. The Spanish (Spain) row explicitly names the app `Laboratorio de baterías Ampere`, unlike the brand `Ampere Battery Lab` shown by the selected Spanish editor in prior checks. Do not publish or combine Submission 61/the 31 listing changes with Submission 62 until that payload discrepancy and translation quality are resolved. The per-row `Weitere Aktionen` menu only offered `View change`; opening it landed in the default German editor and did not alter any listing. A previous browser tab showed a stale/contradictory saved-for-later summary; use a fresh Publishing overview as the current view and recheck immediately before any publication.
- Store-asset eligibility opportunity (2026-09-24 18:58 UTC): Play's live default-listing editor has four phone screenshots and explicitly says at least four screenshots with a minimum resolution of 1080 pixels on each side are required for Google Play promotional eligibility. The existing asset audit measured the four screenshots at 668×1188, so the listing misses this featuring opportunity. The video field is empty. `adb devices -l` found no connected Android device, so no authentic higher-resolution screens could be captured now; do not upscale the old images or use misleading emulator readings. Next actionable step is to capture four genuine 1080×1920-or-higher app screens from a representative device, then inspect them and save as an unsubmitted draft before considering publication.
- Growth and quality refresh (2026-09-24 19:01 UTC / 21:01 Berlin): the authenticated Growth overview, filtered to device / last 28 days, still shows 6 device impressions, 2 acquisitions, and 2 first opens; monthly active devices and 7-day retention are unavailable. The separate last-90-days Store-entry card remains at 66.67% conversion; experiments are 0 running, 0 completed, 0 applied. Android Vitals still has no data for perceived crashes/ANRs, memory, startup/rendering, or battery metrics; lost-user rate is marked `Begrenzte Datenbasis`. These are not zero-crash or retention claims and do not justify a Store Listing experiment.
- SDK warning review (2026-09-24 18:50 UTC): the two 19 Sep SDK notices identify `androidx.activity:activity:1.0.0` and `androidx.fragment:fragment:1.1.0` specifically in Alpha release `0.459`. Their details were expanded and inspected; the notices remain unread and were not deleted. The fixes are included in signed app version `0.489`, which is still under review; do not treat these old-version notices as a new defect in 0.489.
- Play upload workflow recheck (2026-09-24 18:49 UTC): GitHub's latest listed scheduled run remains `36018590931` at 15:13 UTC; manual run `36028309019` successfully uploaded code 489 to the inactive Internal draft, and Alpha attempt `36041284962` failed at its upload step because the package-wide version code was already used. The workflow source now defaults scheduled/manual version to `0.489`; the failed duplicate attempt did not mutate Play tracks. Schedule delivery after 15:13 UTC remains unverified; monitor rather than repeatedly dispatching uploads.
- Tester download check (2026-09-24 19:13 UTC / 21:13 Berlin): the authenticated tester landing page explicitly says `You are a tester` and offers the Google Play install link. The authenticated Console's Testen und veröffentlichen page confirms closed Alpha's active release remains `0.459` (delivery shown 17 Sep 22:37); the fresh Publishing overview still shows Alpha `0.489` as the single change under review in Submission `62`. Therefore this account can install/update the current tester release now, but `0.489` is not downloadable from Play yet. Wait for Submission 62 review approval, recheck its exact contents and publish it under the existing narrow authorization; then allow Play propagation. This does not authorize publishing the separate 31 Store-entry changes or Production.
- Detailed Play growth, listing, translation, and policy audit (2026-09-24 19:26 UTC / 21:26 Berlin): Dashboard still reports 12 closed-test testers for 2 consecutive days; the 14-day gate is incomplete, Production access is disabled, and release status is still gated. Submission activity shows `62` (`Geschlossener Test - Alpha`) `Wird überprüft` and `61` (`Store-Eintrag`) `Bereit zur Veröffentlichung`; Publishing overview separately shows 31 listing changes ready to publish and 489 under review. Do not combine or publish the listing batch.
- Growth overview, filter `Gerät` / `Letzte 28 Tage`: 6 device impressions, 2 device acquisitions, 2 first opens; monthly active devices and 7-day retention are unavailable. The Store listing report shows 3 visitors, 2 single-user install clicks, 67% click-through, while its standard-listing row separately shows 1 visitor and conversion `-`. These are distinct surfaces and extremely small samples; no experiment or conversion conclusion is justified. Store Listing tests remain 0 running, 0 completed, 0 applied. No custom listing is configured; do not create audience targeting without a validated audience or campaign.
- Translation review: order `7430adcd1v2` remains `Abgeschlossen`, machine translation, 28 languages, `0,00 USD`; every locale still offers `Prüfen und übernehmen`, so the order itself has not been imported. `es-ES` in Submission 61's detail names the app `Laboratorio de baterías Ampere`, contradicting the protected brand `Ampere Battery Lab`. Keep the full 31-change set unpublished until the exact payload is reconciled and the translations receive quality review. The separate App-Strings page displayed no string rows or actionable translation controls during this check; do not infer that untranslated UI strings are live or apply an empty operation.
- Policy check: App-Content's `Überprüfung erforderlich` tab says `Alles erledigt`; `Abgeschlossen` lists 11 declarations. The privacy-policy declaration links to `https://github.com/Apfelkringel/ampere-battery-lab-updates/blob/main/PRIVACY.md`; its current public text describes opt-in Firebase Analytics, the data minimization boundary, retention, and withdrawal. The completed Data Safety detail states encryption in transit and automatic deletion over time. No declaration was edited; this spot-check is not a legal-compliance certification.
- Store-media opportunity remains blocked on truthful source material: `adb devices -l` currently lists only emulator `emulator-5554` (`sdk_gphone64_arm64`), not a physical Android device. The existing emulator can produce non-representative/contradictory battery readings, so never use its staged values in marketing screenshots. Capture native 1080×1920-or-larger screenshots from a representative physical device, then review and save as a draft only. Do not upscale the existing 668×1188 assets.
- No Console settings, listing text/assets, experiments, tester membership, or releases were changed during this audit. Prioritized follow-up: (1) exact Submission 62 approval and authorized managed publication; (2) correct/reconcile the `es-ES` payload and review the 31 translations before any listing publication; (3) capture genuine high-resolution phone screenshots; (4) revisit experiments/custom listings only after materially more traffic; (5) recheck Play's quality/pre-launch reports after the reviewed bundle is processed.
- Spanish listing mismatch diagnostic (2026-09-24 19:27 UTC / 21:27 Berlin): reopened the default listing editor and selected `Spanisch (Spanien) – es-ES`. Its current saved fields show app name `Ampere Battery Lab` (18/30), short description `Batería, carga e historial — local, transparente y sin cuenta.` (62/80), and Spanish full description (1,420/4,000); both `Verwerfen` and `Als Entwurf speichern` are disabled, so this editor has no unsaved correction to save. Submission 61's read-only detail still describes its approved change as app name `Laboratorio de baterías Ampere`. The mismatch is therefore between the approved submission payload and current editor state; the ordinary editor does not directly repair the approved queue. No fields were changed. Do not publish submission 61 or remove/recreate its 31 changes until an exact Console correction path and continuity of the other locale edits are established.
- Play-Status-Prüfung ohne Live-Zugriff (2026-09-24 19:53 UTC / 21:53 Berlin): In dieser Ausführung war das Browser-Plugin nicht als steuerbares Tool verfügbar; im Node-REPL fehlte die Browserbindung. CUA fand im lokalen Chrome nur Fenster 114 mit Titel `Neue Funktionen`, nicht den Play-Console-Tab. Es wurde keine Seite navigiert, kein Tab geschlossen und kein Chrome-Profil angehängt; an Play wurde nichts geändert. Das ist keine neue Console-Statusbeobachtung. Der letzte belastbare vollständige Console-Check bleibt 2026-09-24 19:26–19:27 UTC: Submission 62 `Wird überprüft`, aktive Alpha `0.459`; `0.489` war noch nicht für Tester bestätigt. Bei wieder verfügbarer Browserbindung den Einreichungsstatus frisch prüfen und ausschließlich Submission 62 nach Freigabe sowie den dokumentierten Kontinuitätsprüfungen veröffentlichen.
- Play-Veröffentlichungsprüfung (2026-09-24 20:06 UTC / 22:06 Berlin): Im vom Nutzer geöffneten, authentifizierten Play-Console-Tab zeigt die Einreichungsaktivität Submission `62` als `Bereit zur Veröffentlichung`. Die Details nennen `32 Änderungen`: den vollständigen Alpha-Roll-out `489 (0.489)` plus 31 Store-Eintrag-Änderungen. Die spanische (Spanien) Änderung benennt die App `Laboratorio de baterías Ampere`, entgegen der geschützten Marke `Ampere Battery Lab`. Das Veröffentlichungs-Dashboard bündelt sie; es wurde nichts veröffentlicht oder verändert. Alpha `0.459` war im gleichzeitig geprüften Release-Überblick weiterhin für Tester verfügbar. Der Nutzer erlaubte anschließend ausdrücklich eine Veröffentlichung, doch der Umfang ist wegen der gebündelten 31 öffentlichen Listing-Änderungen unklar; vor dem Klick klären, ob alle 32 Änderungen gemeint sind oder nur Alpha `0.489` nach sicherer Trennung der Listings. Bis dahin bleibt `0.459` die Play-Version für Tester.
- Play-Veröffentlichung abgeschlossen (2026-09-24 20:11 UTC / 22:11 Berlin): Nach der ausdrücklichen Bestätigung `Alle 32 Änderungen veröffentlichen` wurde im Managed-Publishing-Dialog die gesamte bestätigte Submission `62` veröffentlicht. Die Console-Übersicht meldet `Zuletzt veröffentlicht am 24. September 2026` und keine unveröffentlichten Änderungen. Der aktive Alpha-Track zeigt `489 (0.489)` als neuesten Play-Release, Status `Für ausgewählte Tester verfügbar`, veröffentlicht am 24. Sept. 22:08 (Console-Anzeige), für 177 Länder/Regionen und 20.367 unterstützte Geräte. Die aufgeklappte Release-Zusammenfassung verlinkt Bundle-Codes `489` und `459`; der kompatible Fallback blieb im Track. Damit ist 0.489 laut Console für geschlossene Tester verfügbar; die Play-Auslieferung auf einzelnen Geräten kann laut Bestätigungsdialog etwa eine Stunde oder länger brauchen. Die 31 Store-Einträge wurden ebenfalls veröffentlicht; `es-ES` enthält im Einreichungsdetail weiterhin den App-Namen `Laboratorio de baterías Ampere`. Keine Internal- oder Production-Veröffentlichung wurde vorgenommen. Der Testerzähler wurde bei dieser Aktion nicht neu geöffnet; letzter verifizierter Stand bleibt 12 Tester für 2 Tage und die 14-Tage-Anforderung unvollständig. Der separat dokumentierte Direct-Release `0.490` wurde nicht in Play Alpha veröffentlicht; eine Play-Einreichung dieser Version benötigt eine separate Release-Prüfung und Autorisierung.
