# AI project context — Ampere Battery Lab

This is a durable, evidence-based map for future agents. It contains repository facts and operational decisions, not runtime user data or private credentials.

## Product scope

Ampere is a local Android battery monitor. It presents battery level, charge state, current, voltage, temperature, power, charging/discharging sessions, health estimates, cycle information, alarms, widget/notification surfaces, optional overlay, and CSV/JSON/TXT exports. Availability depends on OEM and Android APIs; unsupported values remain unavailable.

The iOS port is intentionally narrower: it shows values exposed by iOS and does not pretend to provide Android-only current, capacity, or background monitoring capabilities.

## Source-of-truth hierarchy

1. Runtime behavior and tests in `android/`.
2. `docs/ARCHITECTURE.md`, `docs/VERIFICATION.md`, and `docs/UPDATE-SECURITY.md`.
3. Store/release documentation under `docs/` and `store/`.
4. `README.md` for user-facing scope.
5. Historical commit notes are context, not proof of current behavior.

When sources conflict, inspect current code/tests and document the conflict; do not silently trust an old release note.

## Analytics investigation notes

The code uses Firebase Analytics, not a custom Google Analytics transport. Firebase project configuration is present in `android/app/google-services.json`; actual dashboards and event counts are external and were not available during repository-only analysis.

### Consent flow

1. Startup calls `AnalyticsTracker.restoreConsent()`.
2. No prior grant leaves collection off.
3. Settings exposes the user-facing analytics choice.
4. Granting enables Firebase collection and logs the current section.
5. Withdrawal stores denial, disables collection, and resets analytics data.
6. Battery readings and foreground-app attribution never enter the analytics parameter map.

### Current event inventory

- `ampere_section_view(section=overview|charge|discharge|battery_health|history)`
- `ampere_feature_used(feature=health_measurement|history_export|charge_alarm|discharge_alarm|temperature_alarm|overlay)`

### Questions for an authenticated Firebase/GA read

- How many consenting app instances were active in the last 7/28 days?
- Which sections are viewed most per consenting instance?
- Which allow-listed features are actually used?
- What is the funnel from first section view to health measurement/export?
- Do event volumes differ by app version, Android version, device model, or coarse region, subject to privacy thresholds?
- Are events delayed, missing, or duplicated after consent changes?

Do not infer retention from event totals alone. Separate app-instance counts, event counts, and sessions, and preserve the consent population and retention window in every conclusion.

### Latest verified Analytics snapshot

Source: authenticated Google Analytics/Firebase report for the Ampere property,
read on 2026-09-22. The report range was **25 Aug–21 Sep 2026** (last 28
days), and the report stated that 100% of available data was used for its
cards. This is a snapshot, not a live database export.

- Active users: 69 in 28 days; 69 in 7 days; 1 in the last day.
- Realtime at inspection: 1 active user, country shown as Germany.
- Average engagement time: 1m 56s per active user; 1.4 engaged sessions per
  active user; 1m 17s average engagement per session.
- App stability card: 100.0% users without crashes and 0.0% with crashes.
- Latest app release shown: Ampere Battery Lab `0.468`, status successful.
- MainActivity screen views: 94.
- Revenue: $0.00.

Event report totals for the same range:

| Event | Count | Users | Count per active user |
| --- | ---: | ---: | ---: |
| `ampere_section_view` | 414 | 69 | 6.00 |
| `user_engagement` | 190 | 29 | 6.55 |
| `session_start` | 104 | 69 | 1.51 |
| `screen_view` | 94 | 22 | 4.27 |
| `first_open` | 69 | 69 | 1.00 |
| `ampere_feature_used` | 39 | 18 | 2.17 |
| `app_update` | 22 | 2 | 22.00 |

Interpretation, with the limits of this snapshot: every recorded active user
generated a section-view event, while only 18 users generated a feature event
(about 26% of active users). The six section views per active user suggest
repeated navigation, but do not identify which section without a parameter
breakdown. The 69 first opens and 69 active users indicate a strongly
new-user-heavy 28-day cohort; retention cannot be concluded because the
dashboard snapshot does not provide cohort-day retention. The `app_update`
count is concentrated in only two users and should not be interpreted as 22
distinct upgrades. Investigate event parameters and version dimensions before
changing product decisions.

## Release and distribution facts

- Package/application ID: `com.ampere.batterylab`.
- Current public direct release: `versionCode 489`, `versionName 0.489`. The active Play Alpha remains `0.459` and must not be replaced during the closed-test gate; a public direct release does not change Play tracks.
- Public update manifest URL: `https://raw.githubusercontent.com/Apfelkringel/ampere-battery-lab-updates/main/latest.json`.
- The optional manifest field `releaseNotes_en` localizes update notes for English dialogs and notifications; when it is absent, English surfaces show an English availability message instead of German notes. The example manifest documents both languages.
- Direct APK and Play bundle are separate distribution paths.
- Private signing is mandatory for release builds.

## Latest Play Console inspection (2026-09-22)

- The dashboard shows green checks for the closed-test release and minimum-12-testers gates. The 14-day closed-test gate is not yet complete, and the Production access action remains disabled.
- The active closed track is `alpha`, covers 177 countries/regions, and currently serves release `0.459`, published 18 Sep 2026. The active test release should not be changed casually while continuous tester eligibility is being accumulated.
- Testers are selected through the Google Group `ampere-battery-lab-testers@googlegroups.com`.
- The closed-track feedback channel was set to the public Reddit test thread: `https://www.reddit.com/r/AndroidAppTesting/comments/1whdrqe/looking_for_12_android_testers_ampere_battery_lab/`.
- The feedback-channel change was submitted on 2026-09-22. Submission activity entry 60 (submitted 17:56 as displayed by Console) reached `Veröffentlicht` at 18:16; the Reddit feedback channel is live.
- Next optimization surfaces to inspect in order: pending publication changes, App content/policy declarations, store listing completeness, quality/statistics, App integrity, and release/version alignment.

### Play Console optimization findings

- Policy status currently reports “Keine Sicherheitslücken gefunden”; App content reports no declarations awaiting review.
- The German default store listing is live under the `Tools` category with four phone screenshots. It has no video, website contact, or selected Play Store tags. The current Growth overview (22 Sep 2026, last 28 days) shows 5 device impressions, 0 device acquisitions, and no available data for first opens, monthly active devices, or 7-day retention. The last-90-days Store-entry card shows 66.67% conversion; this is too sparse to drive a copy change or Store-listing experiment.
- On 2026-09-22 the German default listing was opened through its review step. Play Console displayed no visible blocking validation error. Verified current assets: one app icon, one feature graphic, and four phone screenshots; no video or tablet/Chromebook/XR assets are configured. A free Google Play machine-translation order completed for the Store entry in 28 languages (order `7430adcd1v2`, source en-US, 0.00 USD). The generated locale listings were accepted as drafts after restoring the brand name `Ampere Battery Lab` and enforcing the 80-character short-description limit. The publication overview then showed 31 changes and confirmed that all 31 were moved to `Für später gespeichert`; the queue of changes ready to submit is empty. They remain deferred drafts and are not proof of live publication.
- The closed-test feedback-channel change is complete under published submission 60; no binary, version, or track-release change was part of it.
- Submission 60 detail view confirms source `Play Console` and exactly one change: the Alpha tester feedback channel points to the public Reddit thread. No binary, version, or track-release change is part of this review.
- Play protection recheck (2026-09-22): automatic protection is 1/1 active, Google Play Store protection is 6/7 active, and the displayed 30-day card reports 100.0% Google Play installations with unknown sharing `-`. Play Integrity is 0/7 and explicitly not integrated (the seven signals are therefore unavailable). Adding it requires source/backend integration and a new release, so it must not be toggled casually during the active 14-day closed test. Play Billing protection is 0/4 and is not applicable without Play Billing.
- Play Integrity implementation audit (2026-09-22): the Console's dedicated settings page requires linking a Google Cloud project first; all seven response fields are `Aus`, and its Integrity test control is disabled. The repository contains Firebase Analytics but no Integrity client, nonce/token-verification path, server, Cloud Function, or other backend endpoint. Treat this as a planned post-gate engineering project requiring an ownership/privacy decision and a new signed release, not as a missing Play-Console checkbox.
- Notification audit (2026-09-22): the Play Console notification center was opened and read. It confirms the upload-key reset, repeats the two known deprecated-SDK notices (`androidx.activity:activity:1.0.0` and `androidx.fragment:fragment:1.1.0`) for release `0.459`, and shows the publication, identity-confirmation, and optional Google Ads notices. No new technical blocker was found; no notification was deleted.
- Play Console live recheck (2026-09-23): Dashboard still marks the closed-test release and minimum-12 criteria complete, while the continuous 14-day criterion remains in progress and Production access is disabled. The notification center showed the existing 22 Sep upload-key-reset notice, the two SDK warnings scoped to Alpha `0.459`, the 18 Sep publication message, identity confirmation, and one unread 15 Sep Google Ads offer (€400 credit after €400 spend, new advertisers only). No new technical or policy alert appeared; the Ads offer was opened and no campaign was started.
- Play protection detail recheck (2026-09-22): the automatic-protection report shows 100.0% Google Play installations and no measurable unknown sharing; the Play Store protection panel confirms Play App Signing, Play Protect/SDK scanning, spam/anomaly/fairness protections, and only one missing service: Store-listing device checks. Those checks remain disabled because Google warns they can reduce visibility and require a deliberate device-coverage decision; no change was made during tester acquisition.
- Device-check option audit (2026-09-22): the missing Store-protection service is Store-listing device checks. It is disabled, and Google requires acceptance of the device-catalog terms before Basic, Device (recommended), or Strict integrity filtering can be selected. No setting was changed because filtering may reduce visibility and tester reach; decide this after the closed-test gate with explicit device-coverage intent.
- Advanced-distribution audit (2026-09-22): app availability is `Veröffentlicht`; Formfaktoren reports Android XR active. Managed Play Store is disabled; Play-as-you-download is disabled with unavailable controls; carrier targeting is off and Production-only; App Actions is unchecked and requires policy/terms acceptance; App indexing has no confirmed websites; Inline installations are unchecked and require the Inline Install API plus eligibility. No setting was changed because these require corresponding app/deep-link/Assistant capabilities.
- Policy/program audit (2026-09-22): Richtlinienstatus reports `Keine Sicherheitslücken gefunden`, and App-Inhalte reports `Alles erledigt` with no declarations requiring review. `Apps Experience` is locked until a Production release exists; it is a post-gate opportunity, not an actionable closed-test setting. `Von Pädagogen empfohlen` reports `Nicht zulässig`, so no education-program work should be planned for this app.
- Play App-Optimierung (2026-09-22): the Console setting `App-Optimierung` was enabled and saved after Play's confirmation dialog. It applies to future releases generated from App Bundles and does not retroactively alter existing releases or version drafts. This is a low-risk distribution improvement; the active Alpha release remains unchanged.
- Store metadata cleanup (2026-09-22): Play's default German listing flagged the short description because it used a Halbgeviertstrich (`–`) where the policy check requires a Geviertstrich (`—`). The draft now reads `Batterienutzung, Ladestatus und Verlauf — lokal, transparent, ohne Konto.` (73/80 characters). It was saved and re-opened in the review step, which shows the corrected text; publication was not submitted.
- Verification pass (2026-09-22): `gradle --no-daemon :app:testDirectDebugUnitTest :app:lintDirectDebug :app:testPlayDebugUnitTest :app:lintPlayDebug` completed successfully for both distribution flavors. Only the existing SDK XML version-4 compatibility warning and Gradle deprecation/configuration-cache notices were reported.
- Safe next actions: monitor the 14-day gate and Alpha/upload status; later design a Play Integrity integration; then improve store discoverability with evidence-based tags, localization, additional screenshots/video, or a website only when verified assets/content are available.
- Translation quality rule: Google labels the output as machine-generated and not human-reviewed. Spot-check the highest-value locales (German, Spanish, French, Italian, Portuguese, Japanese, Korean, and Chinese) before any combined publication submission; do not blindly submit the mixed asset and localization batch.
- Translation import safeguard (2026-09-23): completed machine-translation orders remain reusable even after a locale has an edited draft. Inspect and compare the existing locale before applying a result; importing can overwrite a better draft.
- Spanish (Spain) order recheck (2026-09-23): order `7430adcd1v2` still offers `Prüfen und übernehmen`. Its machine-generated app name is 30/30 and preserves “Ampere”, but its short description is 116/80 and `Übernehmen` is disabled. The currently saved locale draft already contains `Ampere Battery Lab`, a valid 62/80 short description (`Batería, carga e historial — local, transparente y sin cuenta.`), and a 1,420-character full description identical to the order. The review modal was cancelled without applying anything; no Store text was changed or published.
- French locale correction (2026-09-23): in the authenticated standard listing editor, the `Französisch (Frankreich) – fr-FR` locale showed an Italian short description (`Batteria, carica e cronologia — locale, trasparente, senza account.`; 67/80) alongside a French full description. Replaced only the short description with `Batterie, charge et historique — mesures sur l’appareil, sans compte.` (69/80). Saved as a draft, then reloaded the page, reselected fr-FR, and verified the corrected value persisted. No review submission or publication occurred. This demonstrates that length/brand checks do not catch wrong-language fields; review every field against the selected locale before using or accepting machine translation.
- Localization cross-check (2026-09-23): explicitly selected locale rows and verified the current short-description fields in `it-IT` (67/80), `es-ES` (62/80), `ja-JP` (48/80), `ko-KR` (57/80), `pt-BR` (60/80), `zh-CN` (29/80), `hi-IN` (57/80), `tr-TR` (73/80), and `vi` (65/80). All nine sampled short descriptions were in the displayed locale language and under the 80-character limit. This also confirmed the Italian locale itself is correctly Italian; only fr-FR had the cross-language short-description issue fixed above. No other content was edited and nothing was submitted. This remains a limited language/length check, not native-speaker validation of all translated copy.
- English translation/build regression follow-up (2026-09-23): tests surfaced a missing exact mapping for `Quelle: USB-Ladegerät`, duplicate phrase keys, and an English percentage-spacing expectation mismatch; narrow source/test fixes are in the working tree. A clean both-flavor run of tests, lint, and debug assembly passed. The current uncommitted `android/app/build.gradle` adds `android-classes-jar` as the artifact type for `*UnitTestCompileClasspath`; I did not alter this build change. With that current configuration, a forced recompilation/execution passed 432 Direct and 432 Play unit tests (zero failures/errors/skips). This verifies local compilation/tests/lint/debug packaging but not runtime UI/device behavior or a release. Do not publish the broader uncommitted worktree without device QA and required release/continuity checks.
- Translation validation pass (2026-09-22): all 28 generated locale drafts were opened in the actual Store-entry editor and checked against Play's hard 30-character app-name and 80-character short-description limits. Overlong or brand-inconsistent entries were corrected in Spanish (Spain/Latin America), Czech, Hindi, Hungarian, Indonesian, French, Italian, Dutch, Polish, Portuguese (Brazil), Romanian, Russian, Swedish, Thai, Ukrainian, and Vietnamese. Final direct-editor verification is 28/28 valid: every locale uses `Ampere Battery Lab`, every app name is 18 characters, every short description is 29–80 characters, and no editor shows the limit warning. The 31 resulting listing changes were moved back to `Für später gespeichert`; none were submitted or published. Native-language review remains advisable.
- Play Store settings audit (2026-09-22): app type `App`, category `Tools`, no selected tags. Searches for `Batter` and `Akku` returned no relevant available tag, so no tag was added. The listing exposes the developer email but no verified phone or website. External marketing is enabled; no change was made.

### Security and release automation follow-up (2026-09-22)

- Play Console showed an unread notification confirming the intentionally initiated upload-key reset for `com.ampere.batterylab`; the new upload certificate is announced as valid from 24 Sep 2026 15:37 UTC and uploads are blocked until then. Its SHA-1 exactly matches the tracked `android/ampere-upload-certificate.pem`, so this is the expected release-recovery key rather than an unexplained external reset.
- `.github/workflows/publish-google-play.yml` runs every 15 minutes, verifies the public `latest.json`/AAB hash, validates the Play publisher credential, checks the target track through a temporary Play edit, and skips an upload when the version code is already present. Public manifest verified: version `0.488`, versionCode `488`, AAB SHA-256 `6ca8499a35fb3669b7fc505ce45e08370eaf18901b24fd764deb5fd6bf495c37`.
- The 22 Sep validation run after the edit-API fix succeeded; earlier runs failed on the old track endpoint. The workflow was found `disabled_manually` at 18:57 CEST, so a certificate-activation preflight was added and the workflow was re-enabled at 19:00 CEST. A real internal-track attempt then passed the PEM preflight but Google Play rejected the upload with “recently reset”; the Console activation deadline is authoritative. The preflight now honors both the public upload certificate `notBefore` timestamp and `2026-09-24T15:37:00Z`, and skips safely before either condition. The schedule defaults to `internal`, not the active closed-test `alpha` track.
- Upload workflow hardening (2026-09-22): the manual-dispatch version default/example matches verified public manifest `0.488`; a shared concurrency group serializes scheduled/manual runs, and a 15-minute job timeout prevents stale jobs from overlapping future attempts. The default track remains `internal`.
- Upload workflow regression found and fixed (2026-09-23): schedule-triggered runs have no `workflow_dispatch` inputs, so the deployed workflow used fallback version `0.487` and status `completed` while the manual defaults said `0.488` and `draft`. Runs `35826528277` and `35857598086` failed at the initial public-bundle version check (`RELEASE_VERSION: 0.487`); the upload step was never reached and Play state was not mutated. Commit `5be63d9` aligns both runtime fallbacks to version `0.488` and status `draft`. Validation-only run `35867880435` on that commit succeeded: public bundle/hash and Play API access passed, the target remained `internal`, and upload was safely skipped by the activation preflight through `2026-09-24T15:37:00Z`.
- Track-release preservation update (2026-09-23): live Play Console confirms Internal Testing is inactive and has only saved draft `0.384`/code `467` (20,319 supported devices; setup 1/3). The pinned `r0adkll/upload-google-play` action exposes `versionCodesToRetain` and combines retained codes with the uploaded bundle in the new release. The workflow retains conflicting codes only when every conflicting release is `draft` and the requested new status is also `draft`; if any conflict is non-draft, it skips unless an explicitly reviewed manual override is set. This keeps code `467` in the resulting draft but groups it with the new version rather than maintaining a separate draft entry. YAML and jq matrix cases pass locally. Validation-only run `35877839633` on commit `9e4c628` succeeded, confirmed `VERSION_CODES_TO_RETAIN=467`, and then skipped upload because Google Play's activation deadline is `2026-09-24T15:37:00Z`; the run made no Play track change.
- Live recheck on 2026-09-22: the Dashboard still shows production access disabled because the 14-day closed-test criterion is incomplete; submission 60 is already published and there are no newly submitted changes. The internal test track is shown as inactive. Public manifest version `0.488` must not be described as active on Internal Testing yet; verify the scheduled retry after the Play Console activation deadline has passed.
- Internal-track detail recheck (2026-09-23): Play Console confirms Internal Testing is `Inaktiv`, with a saved unpublished version draft `0.384` (version code `467`, bundle explorer artifact `4860231764308114663`, support for 20,319 Android devices). Draft setup is 1/3 complete; there is no active internal release. This legacy draft is not the public `0.488` bundle and was left unchanged. Workflow inspection: validation-only run `35867880435` succeeded; scheduled runs `35826528277` and `35857598086` failed before the runtime-default fix, and no post-fix scheduled run is listed yet. Continue monitoring; no schedule/track mutation was made.
- Play growth and gate recheck (2026-09-23): Dashboard still confirms the release and minimum-12 closed-test checks, while the continuous 14-day criterion remains incomplete and Production access disabled; update status is “Noch nicht zur Überprüfung gesendet” and Production is inactive. Growth overview (last 28 days) shows 6 device impressions, 2 device acquisitions, and 2 first opens (0% delta); monthly active devices and 7-day retention are unavailable. The separate last-90-days Store-entry card shows 66.67% conversion. Store Listings' 28-day detail report for `2026-08-23`–`2026-09-19` displays 3 visitors, 2 single-user install clicks, and 67% click-through rate; the standard-listing row separately displays 1 visitor and conversion `-`. Do not blend these different periods/surfaces into one conversion funnel without a verified common basis. App Statistics for its last-28-days range through `2026-09-22` lists 2 installed-app users, on 17 and 19 Sep, from Chile and Italy; this is not the Play closed-test opt-in count. Store Listing experiments remain 0 running/0 completed/0 applied. The notification center contains only the known upload-key-reset/SDK/publication/identity/Ads items and no new technical alert. No scheduled GitHub run after the successful validation-only run `35867880435` is currently listed. Given this sparse traffic, no experiment, speculative targeting, or deferred translation/listing publication is justified.
- Store-change queue safeguard (2026-09-23): despite the earlier verified state of 31 changes in `Für später gespeichert` with an empty review queue, the live Publishing overview showed 31 Store-entry changes ready for submission. None was submitted. The explicit `Für später speichern` action was applied to the Store-entry group; Console confirmed `31 Änderungen für später gespeichert` and the review queue returned to its empty-state message. All machine-translation/listing changes remain deferred pending quality/provenance review; the live listing was not changed.
- Feedback and ratings recheck (2026-09-23): Play Console's Testfeedback view contained only explanatory empty-state text and no tester submission. The Ratings report (last 28 days) showed 0 users and no reviews matching the filters, so there is nothing to answer. This is not evidence of zero crashes or positive user sentiment; Android Vitals remains unavailable. Keep encouraging candid private testing feedback via the existing route, never soliciting positive ratings.
- Policy/program recheck (2026-09-23): the live Policy status reports `Keine Sicherheitslücken gefunden`, and App content → `Überprüfung erforderlich` reports `Alles erledigt` with no pending declaration. Apps Experience is explicitly `Gesperrt` until a Production release exists. No declarations or policy settings were changed; Production remains unavailable until the closed-test 14-day criterion is satisfied.
- Google Play protection recheck (2026-09-23): automatic protection remains 1/1 active; Google Play Store protection is 6/7; Play Integrity is 0/7 and not integrated; Play Billing protection is 0/4. The sole inactive Store-protection service is `Geräteprüfungen für Store-Eintrag`; other displayed services (Play App Signing, Play Protect/SDK scans, spam/anomaly/fairness protections) remain active. The 30-day card shows 100.0% Google Play installations and unknown sharing `-`. Device checks remain off because enabling them requires separate device-catalog terms acceptance and may reduce eligible device/virtual-device reach during tester recruitment. No settings or terms were changed.
- Post-release validation (2026-09-22): manual validation run `35765901274` downloaded public `0.488`, verified its AAB hash, authenticated the Play publisher with read-only API access, and safely skipped upload because the Console activation deadline `2026-09-24T15:37:00Z` has not passed. No Play state was mutated. A fresh Dashboard recheck still shows Production inactive, Closed testing active with one track, Internal testing inactive, all release/minimum-12 checks green, and the 14-day criterion incomplete.
- Pre-Launch audit (2026-09-22): Play's Pre-Launch-Bericht page reports `Artefakte hochladen, um Pre-Launch-Berichte zu erstellen` and has no report to inspect. The technical-quality cards in the Testen-und-veröffentlichen overview are still attached to Alpha `0.459`; they are not evidence about the unreleased/public `0.488`. No extra track was created; after the upload-key window, use the planned internal `0.488` upload and inspect the resulting report before any Play release decision.
- Play Console recheck (2026-09-22): Dashboard status is still “Store updates not submitted”; Alpha is the only active test track, Internal Testing is inactive, and Production access remains disabled. The unread reset notification still states the authoritative upload-key activation time `2026-09-24 15:37 UTC`; no newer blocking warning appeared. Because Alpha `0.459` was published 18 Sep 2026, the earliest 14-day checkpoint is approximately 02 Oct 2026; this is a planning date, not proof that the gate has completed.
- Optimization audit (2026-09-22): the Play Console optimization area was reviewed. Device Catalog access is gated by a separate terms-of-service dialog, which was left untouched; Testfeedback has no visible entries; Android Vitals still has no actionable crash/ANR or app-size data; policy status reports `Keine Sicherheitslücken gefunden`; Apps Experience is locked until a production release.
- Store-discovery audit (2026-09-22): the live default listing reports 1 visitor and zero running or completed Store Listing tests. The app remains in category `Tools`; Play's tag manager returned no matching tag for `Batterie` or `Monitor`, so no speculative tag was selected. The developer email is present, while phone and website are empty; no unverified contact data was invented. External marketing is enabled.
- Translation audit (2026-09-22): Google Play machine-translation order `7430adcd1v2` remains completed for 28 languages at `0,00 USD`. The rows still offer `Prüfen und übernehmen`; the generated listing changes remain deferred in `Für später gespeichert` and must receive a deliberate review before submission.
- Play Console live recheck (2026-09-23): the Dashboard still marks the closed-test release and minimum-12 criteria complete, while the continuous 14-day criterion remains in progress and Production access is disabled. The notification center showed the existing 22 Sep upload-key-reset notice, both SDK warnings scoped to Alpha `0.459`, the 18 Sep publication message, identity confirmation, and one unread 15 Sep Google Ads offer (€400 credit after €400 spend, new advertisers only). No new technical or policy alert appeared; the offer was opened and no campaign was started.
- Fresh authenticated Console snapshot (2026-09-23): Dashboard now explicitly reports `Momentan haben sich 12 Tester für 1 Tag angemeldet`. The closed-test release and minimum-12 checks are green, the continuous 14-day criterion remains incomplete, and Production access is disabled. Track summary: Closed testing active (one track); Open testing, Internal testing, and Production inactive. Treat the displayed 12 and 1 day as the current authoritative Console figures, not as evidence that the 14-day requirement is done.
- Growth snapshot (2026-09-23): last 28 days show 6 device impressions, 2 device acquisitions, and 2 first opens (all 0% delta); monthly active devices and 7-day device retention are unavailable. The distinct last-90-days Store-entry card displays 66.67% conversion. Store Listing tests: 0 running, 0 completed, 0 applied. Keep treating these tiny samples as directional only; no experiment or targeting change is justified.
- Notification snapshot (2026-09-23): the notification popup still contains the known 22 Sep upload-key-reset notice, two 19 Sep deprecated-SDK warnings, 18 Sep publication notice, identity confirmation, and 15 Sep Google Ads offer. No new technical alert was visible; no offer or release action was taken.
- Next actions: monitor the 14-day gate and scheduled upload retry, and keep the workflow from targeting `alpha` or `production` without explicit release intent.

### Closed-test recheck (2026-09-22)

- Alpha is still active with release `0.459` (published 18 Sep 2026) in 177 countries/regions. The Tester tab still uses `ampere-battery-lab-testers@googlegroups.com`, the Reddit feedback URL, and the same Android/web participation links.
- The Dashboard's minimum-12-tester criterion remains green, while the Tester tab does not display an exact member or opt-in count. Treat the exact current tester number as unavailable rather than deriving it from group membership or Reddit activity.

### SDK warning remediation (2026-09-22)

- Play Console's 19 Sep notification identifies `androidx.activity:activity:1.0.0` in release `0.459`.
- The current Gradle graph inherited that version transitively through Firebase/Google Play Services. `android/app/build.gradle` now pins stable `androidx.activity:activity:1.13.0`; `dependencyInsight` confirms conflict resolution from `1.0.0` to `1.13.0`.
- Verification passed for both distribution flavors: `testDirectDebugUnitTest`, `lintDirectDebug`, `testPlayDebugUnitTest`, and `lintPlayDebug`.
- The fix is not yet in Google Play; releasing it requires the normal version bump, signed artifact verification, and the closed-test continuity check.

### Android Vitals and size audit (2026-09-22)

- Android Vitals has no available data for user-perceived crashes, ANRs, memory, startup/rendering, battery, or permission-denial metrics. The lost-user metric is explicitly marked as having a limited data basis.
- The App-size page has no representative download/install-size data and no optimization recommendations. Do not interpret these missing values as a clean bill of health; collect more release/user data before making performance claims.
- Technical-quality remediation (2026-09-22): Play's next-release panel flagged transitive `androidx.fragment:fragment:1.1.0` and Android 15 edge-to-edge APIs in Alpha `0.459`. The source now pins stable Fragment `1.9.0`; `dependencyInsight` resolves the old `1.1.0`/`1.0.0` requests to `1.9.0`. `MainActivity` no longer calls Android 15-disabled `setStatusBarColor`, `setNavigationBarColor`, or `setDecorFitsSystemWindows`; its API-35 `WindowInsets` padding remains, and the theme uses a window background instead of deprecated bar-color resources. Direct/Play unit tests, lint, and debug assemblies pass. Signed release `0.488` contains the fix on the public direct/AAB channel; it remains unreleased to Play Alpha pending the continuity-safe release decision.
- Release verification (2026-09-22): signed tag `v0.488` and GitHub Actions run `35764669114` passed metadata validation, Direct/Play tests and lint, signed APK/AAB builds, certificate-lineage verification, provenance attestations, and release attachment. Public update repository commits `20bff3b`, `0ce7ec2`, `86ceb4f`, and `6b74a7a` publish the APK, AAB, `latest.json`, and checksums. Fresh public downloads verified package `com.ampere.batterylab`, version code/name `488`/`0.488`, APK SHA-256 `c2acbfcbf86d40481d5698ba8add3cdbe2d806ef5987d9cc6c2fc0c4f798fec4`, and AAB SHA-256 `6ca8499a35fb3669b7fc505ce45e08370eaf18901b24fd764deb5fd6bf495c37` against `latest.json`.
- Growth/AI audit (2026-09-22): the Play growth overview showed 5 device impressions in the last 28 days, 1 Store-listing visitor, and a displayed 66.67% Store conversion rate; device acquisitions, first opens, monthly active devices, and 7-day retention were unavailable. The built-in Play AI analysis of perceived crash rate by Android version only confirmed coverage of Android 14, 15, and 16 and had no data-backed recommendation because the breakdown was unavailable. The sample is too small for a Store Listing experiment or targeting decision; the experiment setup was inspected but no test was created and no saved listing/translation changes were submitted.

### Manifest and Play-policy audit (2026-09-22)

- `AndroidManifest.xml` targets/compiles against SDK 37, disables cleartext traffic, keeps Firebase Analytics and ad-ID collection disabled by default, and declares no location, contacts, or account permissions.
- The two `specialUse` foreground services carry explicit subtype properties matching local battery telemetry and user-enabled overlay monitoring. Notification, overlay, usage-access, boot, and internet permissions correspond to documented features.
- No new Play App-content declaration was identified from the manifest audit; future permission changes require rechecking the Play forms before release.
- App-content recheck (2026-09-22): the Play Console has no open declarations requiring action and lists 11 completed declarations. Its Data-safety summary reports 3 data types collected or shared, automatic deletion over time, and encryption in transit. This agrees with the current opt-in-only analytics implementation; re-open the declaration before changing SDKs, analytics fields, permissions, or retention.

### Store-copy draft (2026-09-22)

- Saved German short description draft: `Batterienutzung, Ladestatus und Verlauf — lokal, transparent, ohne Konto.` (73/80 characters). This exact text is also synchronized to `store/google-play/listing/short-description.txt`; the Play editor accepted it without the dash-policy warning.
- Publication overview currently holds 31 unsubmitted Store-entry changes (28 locale additions plus German text/media changes) under `Für später gespeichert`. None were submitted; the pre-existing phone-screenshot and feature-graphic drafts remain untouched.
- A local regression guard at `tooling/validate-google-play-metadata.sh` checks the synchronized `app-name.txt` and German short-description source for Play's 1–30/1–80 character limits and dash policy; `.github/workflows/build-apk.yml` runs it before Android tests and release builds.
- `.github/workflows/validate-play-metadata.yml` now runs the same guard on relevant pushes, pull requests, and manual dispatches, providing a fast metadata-only gate before a full signed build is needed.
- Fast metadata CI verification (2026-09-22): push run `35761077182` completed successfully for commit `a36bdc7`.
- App-name guard verification (2026-09-22): push run `35761445345` completed successfully for commit `14a3909`, validating app name `18/30` and short description `73/80`.
- CI verification (2026-09-22): GitHub Actions run `35760526133` passed the metadata guard (`73/80`), direct tests/lint, signed direct APK and Play App Bundle builds, certificate-lineage verification, provenance attestations, and artifact upload. The run completed successfully; GitHub's Node.js/runner migration notices and existing APK/JAR metadata signature warnings were non-blocking.
- CI action maintenance (2026-09-22): updated pinned Actions across Android, iOS, TestFlight, and metadata workflows to verified current releases: checkout `v7.0.1`, setup-java `v6.0.1`, setup-gradle `v6.3.0`, setup-android `v4.0.4`, upload-artifact `v7.0.1`, and attest-build-provenance `v4.2.2`. The Android workflow must be re-run to verify compatibility.
- CI action migration verification (2026-09-22): Android build run `35761765426` passed metadata validation, tests/lint, signed APK/AAB builds, certificate-lineage checks, attestations, and artifact upload with the new pins. The prior Node.js 20 action warnings disappeared; only the independent future `ubuntu-latest` runner migration notice remains.
- Runner stability (2026-09-22): Linux workflows now pin `runs-on: ubuntu-24.04` for Android builds, Play publishing, and metadata validation, avoiding the announced automatic Ubuntu 26 migration. iOS workflows retain their explicit `xcode-27` runner.
- Runner verification (2026-09-22): full Android build run `35762509814` succeeded on the pinned Ubuntu 24.04 runner for commit `7e48bda`. Metadata validation, unit tests/lint, signed APK/AAB builds, certificate-lineage verification, provenance attestations, and artifact upload all passed; tagged-release APK attachment was skipped as expected because the dispatch was from `main`, not a tag. The Ubuntu migration notice is gone; remaining warnings are limited to SDK-manager, Gradle, and JAR-signature metadata details.

### Play Statistics snapshot (2026-09-22)

- In the last-28-days App statistics report, the installed-app audience contained only 2 users, observed on 17 and 19 Sep 2026, split one each between Chile and Italy.
- The crash metric displayed “Daten nicht verfügbar”; no crash-rate conclusion is justified. The sample is too small for store-copy, targeting, or stability decisions.

### Ongoing monitoring (2026-09-22)

- A twice-daily Codex thread monitor `Ampere Play-Console-Gate überwachen` is active with failed-run notifications only (`FREQ=DAILY;BYHOUR=9,18;BYMINUTE=0`, host/local Europe/Berlin schedule). It now checks Submission 62, its narrowly authorized post-approval action, the 14-day gate, Production access, Alpha/upload status, the GitHub Play-upload workflow, unread Play notifications, growth metrics, Store Listing tests/assets, translation/listing drafts, Store-Tag/device-catalog changes, and Play-AI/Vitals results. It must not start an experiment on a statistically tiny sample. Its only current publication authority is Submission 62 under the exact one-change, Alpha-only, code-459-fallback conditions recorded below; it cannot publish Submission 61 or Production.
- The monitor remains silent when state is unchanged and must record material changes in both `AGENTS.md` and this file. It has no authority to publish to Alpha or Production without an explicit, continuity-safe release action.

### Play quality and localization recheck (2026-09-23)

- The authenticated `Abstürze und ANRs` report, date range 26 Aug–23 Sep 2026, has no listed problem entries. Treat this as “no reported items in this view,” not as evidence of zero crashes/ANRs.
- The `App-Größe` page currently shows its explanatory introduction but no download-size measurements or optimization recommendations. Revisit after a representative Play release/device sample exists; do not infer that the bundle is small or optimized from missing data.
- Growth overview rechecked: 6 device impressions, 2 acquisitions, and 2 first opens for the last 28 days; monthly active devices and 7-day retention remain unavailable. The distinct 90-day Store-entry card remains at 66.67% conversion, and Store Listing experiments remain 0 running, 0 completed, 0 applied. These tiny and mismatched samples do not support experimentation or conversion claims.
- Translation order `7430adcd1v2` remains completed for 28 machine-translated languages at `0.00 USD`. Locale rows still offer `Prüfen und übernehmen`; no translation was imported, no draft was submitted, and no live listing changed during this inspection. Keep these drafts deferred until quality/provenance review, prioritizing human/native review for any future market rollout.
- Repeated Publishing overview check: the Console again exposed 31 unsubmitted Store-entry changes after the earlier `Für später speichern` action. No review submission was made; the same group was deferred again and Play confirmed `31 Änderungen für später gespeichert`, returning the review queue to empty. The deferral has resurfaced at least twice, so always recheck the queue immediately before any release/submission and do not assume a prior deferral persisted.

### Track-release draft preservation (2026-09-23)

- Google Play's internal-test track remains inactive with exactly the saved draft release `0.384`, code `467`, artifact `4860231764308114663`; the Console summary shows 20,319 supported devices and 1/3 setup tasks. The draft was not edited in the Console.
- Review of the exact pinned upload action found a supported `versionCodesToRetain` input; its implementation appends retained codes to uploaded codes before writing the draft release. The workflow uses this only when the requested status is `draft` and all conflicting track releases are also drafts. Non-draft conflicts remain blocked by default; the manual override still requires a deliberate continuity review.
- This retains the previous bundle code in the new draft but does not preserve it as a separate release object or guarantee other per-release metadata. Validation-only run `35877839633` confirmed code `467` will be retained, then skipped the upload until key activation; Play was not mutated. After `2026-09-24 15:37 UTC`, verify the first scheduled run and inspect the resulting internal draft before treating the process as healthy. Do not manually upload or replace Alpha/Production releases.

### Managed Play publication (2026-09-23)

- The authenticated Publishing overview showed `Verwaltete Veröffentlichung aus`. Enabled managed publishing and verified `Verwaltete Veröffentlichung aktiviert` remained after page reload.
- Google's documented scope holds most approved changes—including Store listings and full/staged rollouts—until a separate manual publish. Tester configuration and tester-list membership are exceptions; this is not a safeguard for edits to the active closed-test configuration.
- The 31 machine-translation/listing changes remained unsubmitted/deferred; enabling the setting did not submit or publish them. Before any future submission, inspect the full pending queue and manually release only after content review.
- Dashboard gates were unchanged: minimum-12 check green, continuous 14-day criterion in progress, Production access disabled.
- The 31 saved changes resurfaced in the unsubmitted review queue again. Its German short-description row summary showed old en-dash wording, but opening that change in the default listing editor showed the current approved text `Batterienutzung, Ladestatus und Verlauf — lokal, transparent, ohne Konto.` (73/80 characters), matching `store/google-play/listing/short-description.txt`. This proves a stale summary/editor discrepancy, not that the stored listing field itself is wrong. Re-applied `Für später speichern`; Console confirmed `31 Änderungen für später gespeichert` and the review queue returned to its empty state. Nothing was submitted or published.
- Screenshot eligibility audit: Play Console's four current phone assets are `ampere-store-start-no-status-no-system-bars.jpg`, `ampere-store-verlauf-no-status-no-system-bars.jpg`, `ampere-store-sitzung-no-status-no-system-bars.jpg`, and `ampere-store-verlauf-detail-no-status-no-system-bars.jpg`; all are portrait 9:16 at 668×1188 px. They pass the standard listing's 320–3840 px dimension bounds, but miss Google's recommendation-format requirement of four portrait screenshots at 1080×1920 px. This limits eligibility for some recommendation surfaces, not publication of the existing listing. No tablet, Chromebook, or Android XR assets are configured. Do not upscale low-resolution screenshots; a suitable improvement needs native-resolution captures of the current app, then visual/policy review. No connected Android device or emulator was available, so no media was fabricated or uploaded.
- Screenshot capture feasibility test (2026-09-23 17:45 UTC): fresh `:app:assembleDirectDebug` succeeded. Existing Android 16 AVD `ampere-api36` rendered at 1080×1920 after starting with host GPU and 4 GB RAM; host-GPU startup avoided the prior System UI unresponsive overlay seen under software rendering. However, app screenshots showed AVD-generated/internally contradictory measurements (Home 100%/full; Charging +900 mA and 4.5 W; Battery Health 5.00 V, 10 Android-reported cycles, 0 valid sessions). These are not representative physical-device readings, so using them in the public listing would risk misleading users. No screenshot was added to the repository or Play. Reset the AVD display override to 320×640/160 dpi, force-stopped Ampere and shut down without saving a snapshot. Follow up with actual device captures or another verified representative source; never stage battery numbers to satisfy the image-size criterion.
- Upload scheduling: the active 15-minute workflow had no visible scheduled post-fix run at this inspection; latest listed schedule failures were on old commit `502aaeec` and failed at the stale `0.487` manifest-version check before upload. The post-fix validation-only dispatch `35877839633` succeeded and skipped the upload until the accepted key activation time `2026-09-24T15:37:00Z`. Shifted cron from `*/15` to `7,22,37,52 * * * *` to avoid GitHub's documented high-load top-of-hour slot while retaining 15-minute cadence and a check at the activation minute. As of `2026-09-23 15:23 UTC`, no scheduled run had appeared for the first shifted slot at `15:22 UTC`, so this timing adjustment is not yet verified in operation; continue monitoring delay behavior. Track remains internal/draft and Alpha/Production are out of scope.
- Scheduler recheck (2026-09-23 16:39 UTC): GitHub reports the Play-upload workflow active, repository enabled/unarchived, default branch `main`, and the off-hour cron `7,22,37,52 * * * *` present on `origin/main` at `66c56f7`. The six expected schedule slots 15:22, 15:37, 15:52, 16:07, 16:22, and 16:37 UTC produced no listed scheduled run. Latest run is still successful validation-only dispatch `35877839633` (14:56 UTC); latest scheduled run is pre-fix failure `35857598086` (11:58 UTC, `502aaeec`). The repository/default-branch prerequisites are verified, but the reason for missing events is unknown; GitHub's official Actions docs note scheduled events can be delayed/dropped. No workflow dispatch was issued, no upload occurred, and Play state was untouched. Recheck future slots, especially after upload-key activation at `2026-09-24T15:37:00Z`; use a controlled validation-only dispatch only as a temporary fallback, not as proof cron is healthy.
- Validation-only scheduler fallback (2026-09-23 16:46 UTC): manually dispatched workflow run `35891086823` at current `main` (`a74aeed`) using public version `0.488`, `internal`, `draft`, `validate_only=true`, and no release-replacement override. Job completed successfully: public AAB/hash validation passed, Google OAuth and read-only Play API access passed, existing internal draft code `467` was preserved in the prospective draft, and upload-key preflight honored Console activation time `2026-09-24T15:37:00Z`; the Upload bundle step was skipped. No bundle was uploaded and Play track state was not mutated. Manual dispatch provides an operational fallback but does not resolve missing cron events. Authenticated Play Dashboard at 16:47 UTC still shows `12 Tester für 1 Tag`; release/minimum-12 checks pass, 14-day gate remains in progress, and Production access is disabled.
- Schedule follow-up (2026-09-23 16:53 UTC): checked after the `16:52 UTC` cron slot; no schedule run appeared within 90 seconds, and the 16:46 validation-only manual dispatch remains latest. This is the seventh consecutive absent off-hour slot from `15:22` to `16:52 UTC`; workflow active/default-branch prerequisites remain confirmed. The manual run proved bundle/hash and read-only Play credential checks are healthy, so the remaining unresolved issue is schedule event delivery. Do not keep shifting cron without evidence. Continue safe validation-only checks and recheck after the announced key activation; no Play state was changed.
- Schedule follow-up (2026-09-23 17:09 UTC): no run exists for the `17:07 UTC` cron slot; successful validation-only run `35891086823` is still latest. This is the eighth consecutive missing off-hour slot (`15:22`–`17:07 UTC`) despite the active workflow on default branch. Manual validation works; schedule-event creation/delivery remains unresolved. No new dispatch, upload, or Play mutation was performed.
- Store publication queue persistence recheck (2026-09-23 17:12 UTC): Publishing overview still reports Managed publishing enabled, but after reloading it again presented all 31 deferred Store changes under changes not yet submitted, with an enabled `31 Änderungen zur Überprüfung einreichen` button. The approved/publishable changes section was empty. No submission was made. Reapplying the `Für später speichern` action to the Store-entry group produced Console confirmation `31 Änderungen für später gespeichert` and replaced the submit button with that confirmation. Since deferral has resurfaced after reload multiple times, verify the full queue before any submission and never equate Managed publishing with a safe-to-submit draft state.

## Testing and quality risks

- Android OEM battery files and units vary; central validators must remain the only path into calculations and exports.
- Time changes can move sampling windows backwards; sampling policy must reset its basis rather than fabricate rates.
- Android backup/restore and old telemetry formats need normalization.
- Canvas rendering has a large surface area; preserve real accessibility virtual controls when changing visual controls.
- Large-text dashboard navigation is shared through `BatteryLargeTextTabs`; its labels/actions are unit-tested, active-tab state is announced to accessibility services, and newly selected tabs are scrolled into view.
- TalkBack live summaries use locale-specific labels and translate app-generated status, estimate-source, and duration values in English mode; unit tests cover representative German-origin values.
- Alarm thresholds and telemetry intervals have exact English option labels; session detail fields, history-chart bucket fallbacks, and monitor-notification numeric formatting follow the app locale.
- Visible chart scales, current-chart windows, remaining energy, battery power, and charger capability limits now accept the selected numeric locale; export label formatting remains stable for compatibility.
- Localization follow-up: the optional charging screensaver now uses the app-selected language and date locale instead of the device default. Human-readable diagnostic reports support English using the app locale; CSV and research JSON remain locale-independent.
- English UI translation audit (2026-09-23): several visible labels were baked into PNG buttons and bypassed `AppText`, leaving `Aktiv`, `Starten`, and `CSV exportieren` in the English app. Added matching English SVG masters and `drawable-en-nodpi` resources with the same dimensions; widget fallback text now comes from German/English string resources. The health editorial phrase `schafft die Messbasis` also now translates. Keep future embedded labels in locale-qualified assets and include them in the asset hash manifest. This is source-only and has not been released.
- iOS localization follow-up (2026-09-23): the iOS prototype had no localized string resources, and most of its visible copy was German-only. Added German and English `Localizable.strings` resources to the app target, localized all visible screen copy, and explicitly localized dynamic status, shared-summary, and accessibility text. iOS follows the device language; this source change has not been released.
- Android translation follow-up (2026-09-23): filled the remaining exact renderer labels `Dauer` and `Gestartet`, corrected the live-overlay notification copy with atomic phrases, and moved the optional charging-display service label into German/English resources. These fixes remain source-only.
- Play language-delivery fix (2026-09-23): lint identified that an app-specific language switch could outpace Play App Bundle locale splits. Disabled language splitting so the app's English strings and locale-specific button artwork stay installed when a German-device user selects English. Recheck the bundled resources after future localization growth; use Play Core language delivery only if added deliberately.
- Play language-delivery verification (2026-09-23): the rebuilt `app-play-debug.aab` has `BundleConfig.pb` LANGUAGE split `negate=true`, and the English button images are packaged under `base/res`. Lint still emits `AppBundleLocaleChanges` despite that verified bundle setting; treat it as a detector false positive unless a later bundle check shows language splits enabled.
- English translation cleanup (2026-09-24): the Android phrase translator could alter already-English sentences and missed settings, status, and navigation labels; some generated freshness and accessibility text also inherited German persisted values. Added exact translations for the uncovered UI phrases, limited fragment replacements to German-marked text, cached the sorted phrase table, localized dynamic freshness/TalkBack and overlay status text, and protected external app labels. A 320 dp API-36 English UI pass then exposed marker misses in `Live-Anzeige`, `Bildschirm an/aus`, `Lernen braucht`, `Tag/Woche/Monat`, `AUSWERTUNG`, the debug `TESTDATEN` badge, and dynamic `von`/`Testwert`/`von Android gemeldet` fragments. Added exact labels and composed those dynamic values from the selected app language. Direct and Play debug assemblies succeeded; direct-flavor screenshots confirm the affected charging, discharge, health, history-period, and health-source labels now render in English. Emulator battery readings are synthetic and were not used as product evidence. Keep complete English sentences out of legacy fragment translation and review every rendered screen for marker misses. This is source-only; no release was made.
- English translation follow-up (2026-09-24): another API-36/320 dp pass found three marker misses (`am Ende der Entladung`, `Nutzungszugriff ist optional`, `Zugriff einrichten`), untranslated history-chart explanations keyed by `Kennzahl`, and a mixed German/English Android-detection suffix. Expanded the German marker for those unique terms and composed the detection line directly in the selected locale. History-detail dialogs now use complete locale-specific copy, including correct singular/plural wording for measurement intervals. Direct and Play debug assemblies succeeded; screenshots show English overview, charging, discharging, health, settings, history chart, and a history-detail example (`Based on 4 usable measurement intervals`). Emulator battery/history readings remain synthetic UI inputs, not product evidence. Android string-resource key counts match across German/English, and the iOS localization files have matching key counts. This is source-only; no release was made.
- English analysis and accessibility follow-up (2026-09-24): a 320×640 API-36 pass found German technical labels (`Verlust`, `Leistungsaufnahme`, `Energie`, `Vergangene Zeit`, `Restlaufzeit · Screen-on`, `Leistung Min / Ø / Max`) and German average/range captions in dynamic charts. Added exact English mappings and locale-aware chart labels, including `Avg` and `Range`. The pass also found a TalkBack summary corruption (`Temperaturee`) caused by sending an already-English summary through German fragment replacements; the summary and tab description now compose directly in the selected locale. Direct and Play debug assemblies succeeded. Post-change device verification is incomplete: the app encountered an HWUI ANR, and after restarting the API-36 emulator the Android System UI itself became unresponsive. Treat this as an emulator verification failure, not proof of an app-source cause. This follow-up remains source-only and unreleased.
- Background-monitor translation follow-up (2026-09-24): corrected generated heartbeat wording for singular/plural minutes and added English labels for the notification and battery settings links. The links now sit in the scrollable message with one fixed Close action, addressing the clipped footer on a 320×640 screen. `AppTextTest` covers one and two minutes plus the composed explanation; full Direct/Play tests, lint, and debug assemblies passed. The emulator could not complete a fresh visual pass after this dialog-only change because Android System UI remained unresponsive during startup. Xcode is not installed, so the iOS localization was syntax-checked but not built.
- History renderer audit correction (2026-09-24): the earlier note incorrectly described `Datum`/`Typ` as visible in the current wide history UI. Those labels exist only in `drawLegacyHistoryPage`, which `onDraw` does not call; the active page uses `drawHistoryPage` and has no table column headers. Keep the exact English mappings only as fallback if that legacy renderer is restored.
- Check German/English text, small screens, landscape, TalkBack, direct flavor, and Play flavor for user-visible changes.

- Spanish queue/editor discrepancy (2026-09-23): Publishing overview's `es-ES` row still describes the queued app name as `Laboratorio de baterías Ampere`, but the selected Spanish (Spain) editor currently shows `Ampere Battery Lab`; its short description is `Batería, carga e historial — local, transparente y sin cuenta.` (62/80). This may be stale summary text; the editor/row conflict alone cannot establish what payload Play would submit. No content was changed and no submission occurred. Inspect actual change details and resolve this mismatch before ever submitting the 31-change localization batch.
- Post-activation internal AAB upload verified (2026-09-24 16:06 UTC / 18:06 Berlin): after Google's accepted upload-key reset activation time `2026-09-24T15:37:00Z`, started guarded workflow run [36023806899](https://github.com/Apfelkringel/ampere-battery-lab/actions/runs/36023806899) with `0.488`/code `488`, `internal`, `draft`, `validate_only=false`, and `allow_track_release_replacement=false`. Run succeeded: public bundle/`latest.json` hash matched; publisher OAuth/read access passed; preflight found only existing internal draft code `467` and retained it; upload action logged `Successfully uploaded 1 artifacts` and Play Edit `05388693841868255179` committed successfully. Independent authenticated Console inspection of `/tracks/internal-testing` shows Internal Testing still `Inaktiv`; the sole draft card remains labelled `0.384`, but its version-code details link to both `488` (new Bundle Explorer artifact `4860233712402175355`) and `467` (existing artifact `4860231764308114663`), with 20,340 supported devices. Thus code `488` is staged as an internal draft, not yet available to testers; track setup remains 1/3 tasks complete. Alpha `0.459` is unchanged and Production remains inactive. Dashboard reports 12 closed-test testers for 2 consecutive days; the 14-day gate is incomplete and Production access disabled. Publishing overview still shows 31 deferred listing/translation changes; none were submitted because the Spanish app-name queue summary still conflicts with the editor's `Ampere Battery Lab` and locale quality needs review. No track activation, Alpha/Production publication, or listing submission was performed.
- Background-monitor localization/UI follow-up (2026-09-24): corrected generated freshness wording to use singular/plural forms and updated the monitor-help dialog so its settings links remain reachable in a scrollable body with a fixed Close action. Regression tests cover both strings and dialog copy. Direct/Play unit tests, lint, and debug assemblies passed before the release. A final visual recheck of this dialog was blocked by an unresponsive emulator; do not claim the latest dialog layout was visually confirmed. The iOS localization files passed `plutil -lint`; a full iOS build was unavailable because this machine has Command Line Tools but no full Xcode installation.
- Public direct release 0.489 (2026-09-24 16:09 UTC): signed tag `v0.489`, Android workflow [36025102250](https://github.com/Apfelkringel/ampere-battery-lab/actions/runs/36025102250), and public update-repository commit `e6b1b45` published the APK, AAB, bilingual `latest.json`, and checksums. CI passed metadata validation, Direct tests/lint, signed APK and Play AAB builds, signer-lineage verification, provenance attestations, and release attachment. Fresh public artifacts verified as package `com.ampere.batterylab`, version code/name `489`/`0.489`, APK SHA-256 `239b054cc1ae4d613ebc14cc6508e1de9112e8adc94de8a70f96a44fa7b7d29e`, and AAB SHA-256 `6c658e554788aa6bc75041f2f79a355bcc5293def03472b38dfc4cca27a5f69a`; both match the manifest. The English release notes are present. The release includes the Android translation corrections and background-monitor wording/dialog fix. It did not publish to Play Alpha or activate Internal Testing.
- Android developer verification key added (2026-09-24 16:17 UTC / 18:17 Berlin): the authenticated Console showed package `com.ampere.batterylab` registered with three confirmed signing fingerprints, none matching the current direct-release certificate documented in `docs/UPDATE-SECURITY.md`. Added its public SHA-256 fingerprint (`FA:29:B8:75:95:EF:1B:34:B2:06:9D:1E:28:42:D2:11:4B:15:52:A0:74:E0:22:EE:52:7A:7E:C1:7E:98:1A:D3`) through the package-key manager. Console now lists four keys; the new one is `Wird überprüft`, not yet confirmed. This is only a public certificate association; check later for `Bestätigt` or an ownership-proof request. No app release or testing-track state changed.
- Android developer-key verification completed (2026-09-24 16:22 UTC / 18:22 Berlin): the package-key manager now shows the direct-release public certificate fingerprint `FA:29:B8:75:95:EF:1B:34:B2:06:9D:1E:28:42:D2:11:4B:15:52:A0:74:E0:22:EE:52:7A:7E:C1:7E:98:1A:D3` as `Bestätigt`; all four keys for `com.ampere.batterylab` are confirmed. The developer-verification app-list says all Play apps are registered. The earlier pending-review state is resolved.
- Play 0.489 internal draft uploaded (2026-09-24 16:34 UTC / 18:34 Berlin): guarded workflow [36028309019](https://github.com/Apfelkringel/ampere-battery-lab/actions/runs/36028309019) ran with `0.489`, `internal`, `draft`, `validate_only=false`, and replacement override false. It verified the public AAB/hash and Play access, confirmed the upload key was active, preserved draft codes `467,488`, uploaded the new bundle, and committed Play Edit `05370575271627213330`. Authenticated Console verification shows the internal track is still `Inaktiv`, setup is 1/3, and its draft remains labelled `0.384` while containing code `489` (artifact `4860233721511506227`), `488` (`4860233712402175355`), and `467` (`4860231764308114663`), supporting 20,367 devices. Alpha remains `0.459`; Production is inactive. The new bundle is only an inactive internal draft and has not reached testers. No Alpha/Production publication or Internal activation occurred.
- Current gate, growth, and quality snapshot (2026-09-24 16:22 UTC / 18:22 Berlin): Dashboard remains at 12 testers for 2 consecutive days, with the 14-day requirement incomplete and Production access disabled. Alpha `0.459` is still a full rollout to 177/177 regions (Console installation-base card 33.33%). Last-28-day growth remains 6 device impressions, 2 acquisitions, and 2 first opens, each 0% delta; monthly active devices and 7-day retention are unavailable; last-90-day Store conversion remains 66.67%. Listing experiments remain 0 running, 0 completed, 0 applied. Crash/ANR view showed no surfaced active problem rows in its last-28-day view; this is not proof of zero crashes. Pre-Launch still says `Artefakte hochladen, um Pre-Launch-Berichte zu erstellen`; no report was present shortly after the 0.489 internal-draft upload.
- Submission 61 is ready to publish, not live (checked 2026-09-24 16:22 UTC / 18:22 Berlin): Console has a new unread 24 Sep approval notice. Submission activity records ID `61`, a Store-entry submission made 24 Sep at 17:57 Berlin, containing 31 changes and status `Bereit zur Veröffentlichung`. Publishing overview still has managed publishing enabled, offers `31 Änderungen veröffentlichen`, and reports last live publication on 22 Sep. In the actual submission details, the `es-ES` app name is `Laboratorio de baterías Ampere`, conflicting with the editor's previously observed live value `Ampere Battery Lab`; the discrepancy is in the approved queued payload itself. This agent did not submit or publish it. Keep submission 61 unpublished until the Spanish brand name is corrected and the machine-translated locale batch is reviewed; do not equate Play approval with permission to publish.
- Scheduled upload cadence recheck (2026-09-24 16:33 UTC): latest scheduled `publish-google-play.yml` run remains `36018590931` (15:13 UTC), while latest prior manual run is `36023806899` (15:56 UTC). No later scheduled run was visible. The guarded manual 0.489 internal-draft upload succeeded as `36028309019`; it validates the current Play path but does not establish that the periodic schedule recovered.
- Scheduled Play-upload recovery check (2026-09-24 07:00 UTC): after the prior run of missed off-hour cron slots, GitHub now lists four successful schedule-triggered executions on commit `a74aeedf`: `35907072918` (19:06 UTC), `35927228603` (22:14 UTC), `35938926959` (00:33 UTC), and `35959280961` (05:17 UTC). Inspected latest run `35959280961`: public `0.488` AAB and manifest hash matched; Google OAuth/read-only Play API access passed; internal draft code `467` was retained; upload key was correctly rejected by the workflow's time gate until Console activation `2026-09-24T15:37:00Z`, so upload was skipped. The run succeeded but observed intervals were irregular; this confirms cron events resumed, not that every 15-minute slot is delivered. No AAB upload or Play track mutation occurred. Authenticated Dashboard still says `12 Tester für 1 Tag`; the 14-day closed-test criterion is incomplete and Production access is disabled. Continue monitoring and inspect the internal draft only after the activation deadline; do not alter Alpha/Production.
- App Statistics refresh (2026-09-24 07:00 UTC): authenticated `App-Statistiken` report for `Letzte 28 Tage` (2026-08-27–2026-09-23), metric `ACTIVE_USERS` / installed-app audience / unique users per day / country, now has a new row for 20 Sep with 3 users total (Chile 1, India 1, Italy 1); rows for 17–19 Sep remain 2 users/day (Chile 1, Italy 1). Compared with the 23 Sep report, this is one new usage-date signal; it is not a Play closed-test opt-in or retention measure. The `Mehr Nutzer gewinnen` overview remains 6 device impressions, 2 device acquisitions, 2 first opens, monthly active devices unavailable, and 7-day device retention unavailable; separate last-90-days Store-entry conversion remains 66.67%; Store Listing experiments remain 0 running/0 completed/0 applied. Traffic is still far too sparse to start an experiment or infer a stable conversion/retention trend.
- Deferred Store-change queue recheck (2026-09-24 07:00 UTC): Publishing overview again showed 31 unsubmitted Store-entry locale changes under `Änderungen noch nicht zur Überprüfung eingereicht` and an enabled `31 Änderungen zur Überprüfung einreichen` button, despite the earlier saved-for-later confirmation. Managed publishing remained enabled; the overview still said last published 22 Sep 2026. No changes were submitted. Reapplied the Store-entry group's `Für später speichern`; Console then confirmed `31 Änderungen für später gespeichert` and displayed the empty state for changes ready to submit. The `es-ES` row still summarizes the app name as `Laboratorio de baterías Ampere`, conflicting with the previously rechecked editor's `Ampere Battery Lab`; do not submit this batch until the mismatch is resolved and locale content is reviewed.
- Upload-key window precheck (2026-09-24 13:10 UTC): scheduled GitHub run `35985549783` on `main` commit `a74aeedf` completed successfully at 10:09 UTC. Its log explicitly passed release `0.488` public artifact/manifest hash checks and Play OAuth/read-only API access, and identified internal draft code `467` for retention. It skipped bundle upload because Console activation remains `2026-09-24T15:37:00Z`. The key window has not arrived at this observation; do not bypass the workflow's guard or manually upload before activation. Recheck after 15:37 UTC and inspect the resulting internal draft; this is not authorization to touch Alpha or Production.
- Upload-key window recheck (2026-09-24 14:46 UTC / 16:46 Berlin): the authenticated Publishing overview still reflects managed publishing enabled and last publication on 22 Sep. The accepted key-reset notice's activation deadline is `2026-09-24T15:37:00Z` (17:37 Berlin), approximately 51 minutes after this observation. GitHub Actions' workflow page still shows run `35985549783` (#121, scheduled, success, 24 Sep 12:09 GMT+2 / 10:09 UTC) as newest; job steps show public artifact/hash verification, Play publisher credential validation, and certificate-time preflight passed, while the actual upload step was skipped. Thus no post-activation upload is yet verified. Allow the existing `internal`/`draft` schedule to run after activation, then verify the created internal draft and retained code `467`; do not dispatch a live upload outside its guard or touch Alpha/Production.
- Manual validation-only run during key wait (2026-09-24 14:56 UTC / 16:56 Berlin): GitHub Actions still showed scheduled run #121 (`35985549783`, 10:09 UTC) as newest despite the workflow's `7,22,37,52 * * * *` cadence. Started run `36016439739` (#122) with `0.488` / `internal` / `draft`, `validate_only=true`, and release-replacement override disabled. It completed successfully: public AAB/`latest.json` version and hash matched; service-account OAuth and Play read access succeeded; the internal track still contained draft code `467`, which the workflow would retain. The log says the key was not usable before `2026-09-24T15:37:00Z`; `Upload bundle to Google Play` was skipped. Because `validate_only` was true and the pre-activation time guard independently required skipping, this is not a real-upload test. The temporary Play edit was deleted and no track state changed. After 15:37 UTC, recheck scheduled-run cadence and the internal track; if scheduled execution is absent, use the existing guarded process only after verifying current track continuity. Never target Alpha/Production.
- Spanish listing editor spot-check (2026-09-24 15:12 UTC / 17:12 Berlin): the live `es-ES` editor currently shows app name `Ampere Battery Lab` (18/30), Spanish short description `Batería, carga e historial — local, transparente y sin cuenta.` (62/80), and a populated localized full description. This does not match the Publishing queue's summary `Laboratorio de baterías Ampere`, so the mismatch may be stale queue metadata, but the editor view alone cannot prove the queued change payload. No field was edited or saved; no listing was submitted. Keep all 31 translation/listing changes deferred until change details establish the exact payload and all locales receive quality review.
- Scheduled Play workflow recovery (2026-09-24 15:13 UTC): GitHub API now lists scheduled run `36018590931` (#123), created 15:13:34 UTC, about 6m34s after the configured `:07` slot. Its completed-successful job log confirms public version/hash validation, publisher OAuth and read-only Play access, and retention of internal draft code `467`; the upload-key guard still skipped the upload until Console activation `2026-09-24T15:37:00Z`. This shows scheduled execution resumed after the earlier 10:09 UTC run, but cadence remains irregular and missing slots cannot be treated as successful checks. Verify the first successful run after the exact activation deadline and independently inspect the internal track for the uploaded draft.
- Deferred Store-change queue resurfacing (2026-09-24 14:46 UTC): despite the earlier saved-for-later confirmation, the Publishing overview again showed 31 locale/listing changes in its not-yet-submitted group and an enabled submit button. The Spanish (Spain) summary still names the app `Laboratorio de baterías Ampere`, conflicting with the previously reviewed editor value `Ampere Battery Lab`. No changes were submitted. Reapplied the exact Store-entry group action `Für später speichern`; Console confirmed `31 Änderungen für später gespeichert` and showed the empty state for changes ready for review. Recheck and defer the same batch at future monitoring passes until translation quality and the Spanish-name discrepancy have been resolved. No live listing or track release was changed.

## Autonomous Google Play improvement log

- Operating rule (2026-09-24): for the user's request to improve Play Console opportunities independently, keep a prioritized, evidence-based backlog and complete reversible, draft-only fixes when their effect is clear. Record source page, timestamp, exact before/after, and whether the outcome is draft, approved, or live. General autonomy does not authorize track rollouts, tester-track enrollment changes, Production publication, legal-term acceptance, device filters that may reduce reach, paid advertising, notification deletion, or underpowered experiments. Narrow exception: the user explicitly asked for the newest version in the closed tester track; after review, Submission 62 alone may be published only if it still contains the single Alpha full rollout of code 489, code 459 remains as compatibility fallback, and no unrelated changes are included. If any condition is unclear, stop and ask.
- Closed-test/release state rechecked at 2026-09-24 18:09 UTC / 20:09 Berlin: Dashboard reports 12 opted-in testers for 2 consecutive days; the 14-day gate is incomplete and Production access remains disabled. Closed Alpha still serves `0.459`, last updated 17 Sep. Internal Testing is inactive, only 1/3 setup tasks are complete, and its mislabeled `0.384` draft contains codes `489`, `488`, and `467`; 0.489 is not available to testers through Play. Do not activate Internal for anyone in the minimum-12 group: internal opt-in disqualifies them from closed testing. An Alpha update remains a distinct release decision requiring continuity review.
- Draft-only metadata correction (2026-09-24 18:09 UTC): Play's Finnish (`fi-FI`) short-description field flagged an en dash under its metadata check. Replaced it in the Store-entry draft with `Akun käyttö, lataustila ja historia — paikallinen, läpinäkyvä, ei tiliä.` (72/80), saved as a draft, then reopened the locale and verified the exact text persisted without the dash-rule warning. This did not publish or alter the 31-change approved queue.
- Listing queue and notifications rechecked (2026-09-24 18:09 UTC): managed publishing remains enabled; last live publication is 22 Sep; submission 61's 31 Store-entry changes still have a `31 Änderungen veröffentlichen` action. The `es-ES` row continues to name the app `Laboratorio de baterías Ampere`, while the selected editor shows `Ampere Battery Lab`; the discrepancy remains unresolved, so keep all 31 changes unpublished pending payload reconciliation and locale review. Notification center still contains the unread 24 Sep approval notice and the two known 19 Sep deprecated-SDK notices. Read the approval text; no notification was deleted and no publication occurred.
- Store-entry performance data surfaced in the retained 2026-08-23–2026-09-19 report: 3 visitors, 2 single-user install clicks, and 67% click-through; the standard-entry row separately shows 1 visitor and conversion `-`. These are distinct report surfaces and must not be combined into a single funnel. The sample is far too small for a Store Listing experiment or broad copy/targeting decision.
- Next safe review order: reconcile the `es-ES` approved-payload/editor mismatch; compare remaining locales against product claims and Play limits; verify the Finnish correction is included in any future reviewed submission; refresh the metric period before interpreting growth; continue preserving Alpha tester continuity. Do not publish submission 61 or activate Internal as a shortcut to make 0.489 downloadable.
- Live Console recheck (2026-09-24 18:49 UTC / 20:49 Berlin): Submission `62` remains `Wird überprüft` and its detail page lists exactly one change: closed Alpha `489 (0.489)`, full rollout. Dashboard says 12 opted-in testers for 2 consecutive days; the 14-day criterion is incomplete and Production access is disabled. The 24 Sep approval notification is still present, but submission activity shows it is submission 61 (the Store-entry batch) that is `Bereit zur Veröffentlichung`; it does not approve submission 62. The live tester-track version remains 0.459.
- Publishing queue and localization hold (2026-09-24 18:49 UTC): a fresh Publishing overview shows the 31 Store-entry changes under `Änderungen, die veröffentlicht werden können` with `31 Änderungen veröffentlichen`, separate from Submission 62 under review. The Spanish (Spain) entry's exact row still says `Laboratorio de baterías Ampere`, inconsistent with the selected editor previously showing the protected brand `Ampere Battery Lab`. Keep submission 61 and all 31 listing changes unpublished until the payload mismatch and language quality are reconciled. The row's `View change` action opened the standard German editor and made no changes. A stale prior tab displayed a conflicting saved-for-later indicator; a fresh page's explicit 31-change publish group is the current evidence.
- Store-asset eligibility gap (2026-09-24 18:58 UTC): the Play listing editor shows four phone screenshots and says featuring requires at least four screenshots with at least 1080 pixels on each side. Prior image audit measured current shots at 668×1188; they therefore do not meet that recommendation eligibility threshold. Video is unset. No Android device was connected (`adb devices -l` returned an empty device list), so no genuine replacement screenshots were captured or uploaded. Do not upscale or stage misleading readings; capture representative 1080×1920+ app screens on a real device, inspect them, and save an unsubmitted draft before considering publication.
- Growth/Vitals refresh (2026-09-24 19:01 UTC / 21:01 Berlin): authenticated Growth overview (device, last 28 days) remains at 6 impressions, 2 acquisitions, and 2 first opens; monthly active devices and 7-day retention are unavailable. The separate 90-day Store-entry conversion is 66.67%; Store Listing tests remain 0 running, 0 completed, 0 applied. Android Vitals still has no numeric data for perceived crashes/ANRs, memory, startup/rendering, or battery; lost-user rate is marked `Begrenzte Datenbasis`. No experiment or stability claim is justified.
- SDK notice details inspected (2026-09-24 18:50 UTC): the two 19 Sep notices identify `androidx.activity:activity:1.0.0` and `androidx.fragment:fragment:1.1.0`, both specifically in Alpha `0.459`. Their details were expanded; the notices remain unread and were not deleted. The fixes are in signed 0.489, still under review.
- Upload workflow recheck (2026-09-24 18:49 UTC): GitHub's newest scheduled run is still `36018590931` (15:13 UTC); successful manual `36028309019` uploaded 0.489 to the inactive Internal draft. Manual Alpha attempt `36041284962` failed at bundle upload because code 489 had already been used package-wide; no Play track changed. Current workflow defaults to 0.489; post-15:13 schedule delivery is unverified, so monitor instead of repeatedly retrying.
- Closed-Alpha 0.489 submission (2026-09-24 18:42 UTC / 20:42 Berlin): user explicitly wants the newest app available to closed testers. At the 18:22 Berlin Dashboard check, Play showed 12 opted-in testers for 2 consecutive days; the 14-day requirement remained incomplete and Production access disabled. Workflow run `36028309019` had already uploaded code `489` to the inactive Internal draft; a subsequent Alpha workflow attempt (`36041284962`) failed safely because Play version code 489 was already used, with no track mutation. Created the Alpha draft from the existing Play bundle library and retained active code `459` as fallback. Console preview showed no devices losing support: new 0.489 requires API 24+, while 0.459 remains API 23+ compatible. Preview showed only nonblocking missing native debug-symbol warnings. Submitted exactly the single Alpha release `489 (0.489)` as Submission `62`, full rollout; at 18:42 UTC the submission status was `Wird überprüft`. Managed publishing is enabled. Once approved, the next authorized step is to verify Submission 62 still contains only this intended Alpha rollout and manually publish that submission; this specific user request authorizes publishing this exact Alpha release after review. It does not authorize Production, activating Internal testing, or publishing Submission 61 / the separate 31 Store-entry changes. Until Google review and managed publication finish, the live tester-track version remains 0.459 and 0.489 is not yet downloadable from Play.
- Tester download check (2026-09-24 19:13 UTC / 21:13 Berlin): the authenticated tester landing page says `You are a tester` and links to the Play listing. The Console's Testen und veröffentlichen overview confirms the active closed Alpha release is `0.459` (delivery shown 17 Sep 22:37); a fresh Publishing overview still lists the single Alpha `0.489` full-rollout change under review as Submission `62`. Tester access is working, and the current release can be installed/updated, but 0.489 cannot yet be downloaded from Play. After review, revalidate that Submission 62 contains only the intended Alpha change and publish it under the user's existing narrow authorization; then allow Play propagation. The separate 31 Store-entry changes and Production remain out of scope.
- Detailed Play growth, listing, translation, and policy audit (2026-09-24 19:26 UTC / 21:26 Berlin): Dashboard still reports 12 closed-test testers for 2 consecutive days; the continuous 14-day gate remains incomplete and Production access disabled. Submission activity confirms `62` (`Geschlossener Test - Alpha`) is `Wird überprüft`; `61` (`Store-Eintrag`) is `Bereit zur Veröffentlichung`. Publishing overview has 31 listing changes ready to publish, separately from Alpha `0.489` under review. No changes were submitted or published in this audit.
- Growth evidence (device, last 28 days): 6 device impressions, 2 device acquisitions, and 2 first opens; monthly active devices and 7-day retention unavailable. The Store listing report separately shows 3 visitors, 2 single-user install clicks, and 67% click-through, while the standard-listing row shows 1 visitor and conversion `-`. Do not merge the differing report surfaces into one funnel. Store Listing experiments remain 0 running / 0 completed / 0 applied. Traffic is too sparse for experiments, stable conversion claims, or custom-listing targeting.
- Translation order `7430adcd1v2` is still completed machine translation for 28 languages at 0.00 USD; each locale continues to show `Prüfen und übernehmen`, so the order has not been imported. The exact `es-ES` row in Submission 61 names the app `Laboratorio de baterías Ampere`, not the protected brand `Ampere Battery Lab`. Keep the 31-change listing batch unpublished until the Spanish payload is reconciled and locale quality is reviewed. The App-Strings page showed no string rows or actionable translation controls in this session; that is a UI observation, not proof about delivered app resources.
- App-content/privacy spot-check: the required-review tab says `Alles erledigt`; 11 declarations are listed as completed. The saved privacy-policy URL is `https://github.com/Apfelkringel/ampere-battery-lab-updates/blob/main/PRIVACY.md`; the linked text currently describes opt-in Firebase analytics, minimized event collection, retention, and withdrawal. The Data Safety detail states encryption in transit and automatic deletion over time. No declarations were edited; this is not a legal-compliance certification.
- Store media: `adb devices -l` now lists only emulator `emulator-5554` (`sdk_gphone64_arm64`), with no physical device listed. Earlier emulator use produced internally contradictory battery readings, so do not stage or use its values in public Store screenshots. The 668×1188 current assets should be replaced only with genuine native-resolution captures (at least 1080×1920 for the noted featuring opportunity) from a representative physical device; inspect and save any replacements as drafts before publication.
- Prioritized next steps: (1) recheck Submission 62 and, only when approved, verify the single intended Alpha `489` rollout plus `459` fallback before the already-authorized managed publication; (2) reconcile the approved `es-ES` payload and quality-review all 31 translations; (3) obtain truthful high-resolution phone assets; (4) wait for materially more acquisition traffic before Store Listing experiments/custom targeting; (5) recheck Vitals and pre-launch reports after the reviewed bundle is processed. No release, listing, privacy, or experiment setting changed in this audit.
- Spanish listing mismatch diagnostic (2026-09-24 19:27 UTC / 21:27 Berlin): reopened the standard listing editor and selected `Spanisch (Spanien) – es-ES`. The current saved editor shows the protected app name `Ampere Battery Lab` (18/30), valid Spanish short description `Batería, carga e historial — local, transparente y sin cuenta.` (62/80), and a Spanish full description (1,420/4,000). `Verwerfen` and `Als Entwurf speichern` are disabled, so no unsaved field correction exists. Submission 61's read-only details still list the approved payload name `Laboratorio de baterías Ampere`. This is a current-editor versus approved-submission mismatch, not a typo that can safely be saved from this editor. No field was edited; keep submission 61 unpublished and do not remove/recreate the 31-change batch until a precise correction path and preservation of other locale changes are established.

### Play-status attempt without live access (2026-09-24 19:53 UTC / 21:53 Berlin)

In this execution the Browser plugin was not available as a controllable tool and the Node REPL had no initialized browser binding. CUA found only local Chrome window 114 titled “Neue Funktionen”, not the Play Console tab. No page was navigated, no tab was closed, no Chrome profile was attached, and no Play state was changed. This is not a new Console observation. The last full authoritative Console check remains 2026-09-24 19:26–19:27 UTC: Submission 62 was “Wird überprüft”, live Alpha was 0.459, and 0.489 was not yet confirmed available to testers. When the Browser binding is available again, recheck the submission and publish only Submission 62 after approval and the documented continuity checks.

## Files to read first

- `AGENTS.md` — operating rules and completion gates.
- `README.md` — user-facing feature inventory and development notes.
- `docs/ARCHITECTURE.md` — system boundaries and persistence model.
- `docs/PROJECT-STRUCTURE.md` — repository map and build locations.
- `docs/STORE-PUBLISHING.md` — Play/iOS publishing requirements.
- `docs/UPDATE-SECURITY.md` — update verification and signer policy.
- `android/app/src/main/java/com/ampere/batterylab/AnalyticsTracker.java` — analytics contract.
- `android/app/src/main/java/com/ampere/batterylab/MainActivity.java` — UI/lifecycle integration.
- `android/app/src/test/` — executable behavior and regression coverage.
- English session-history data-loss fix and direct release `0.490` (2026-09-24 20:00 UTC / 22:00 Berlin): the monitor had written English dates with an embedded comma and durations as `hr/min`, while session loading split on commas and validated only German `Std./Min.` durations. New dates are delimiter-safe; both locale duration formats validate; normalization repairs older split English dates. Direct and Play debug variants each passed 443 unit tests, lint, and APK builds. On the emulator, a synthetic legacy session rendered as `Sep 24 · 8:30 PM`, `Charging`, `+12%`, `1 hr 25 min`; its 18-field legacy form was normalized to 17 fields. The original emulator preference file was restored byte-for-byte afterward. The release workflow `36051515279` built package `com.ampere.batterylab`, code/name `490`/`0.490`, signed the APK and AAB, verified the signer lineage, and attached provenance. Source commit `d85b9dd` is tagged `v0.490`; public update-repository commit `2e27c64` publishes both artifacts and bilingual release notes. Publicly downloaded files were matched against `latest.json`: APK SHA-256 `e8664973e7b86dc42bdc17c6f39c17b3427d726b83832dc56ec8c0b34f0ccbf9`; AAB SHA-256 `92a0229f92f77f67a7d8256ca06940ca7142982b216e74fd2c57f5a91a9abc9c`. This publishes the direct update channel; it does not publish the bundle to Play. The latest full Console observation remains 2026-09-24 19:26–19:27 UTC, when Alpha `0.459` was active and Submission `62` for `0.489` was under review; Play status was not live-checked during the 0.490 release.
